(* Generated by cdp-gen from Chrome DevTools Protocol r1698617. DO NOT EDIT. *)

[@@@ocaml.doc "Supports additional targets discovery and allows to attach to them."]

include Cdp_target_types
open Jsonkit.Primitives

module Activate_target = struct
  let name = "Target.activateTarget"

  type params = { target_id : Cdp_base.Target.Target_id.t [@key "targetId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Activates (focuses) the target."]

module Attach_to_target = struct
  let name = "Target.attachToTarget"

  type params = {
    target_id : Cdp_base.Target.Target_id.t; [@key "targetId"]
    flatten : bool option;
       [@key "flatten"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Enables \"flat\" access to the session via specifying sessionId attribute in the commands.\n\
          We plan to make this the default, deprecate non-flattened mode,\n\
          and eventually retire it. See crbug.com/991325."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    session_id : Cdp_base.Target.Session_id.t; [@key "sessionId"] [@ocaml.doc "Id assigned to the session."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Attaches to the target with given id."]

module Attach_to_browser_target = struct
  let name = "Target.attachToBrowserTarget"

  type result = {
    session_id : Cdp_base.Target.Session_id.t; [@key "sessionId"] [@ocaml.doc "Id assigned to the session."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Attaches to the browser target, only uses flat sessionId mode."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Close_target = struct
  let name = "Target.closeTarget"

  type params = { target_id : Cdp_base.Target.Target_id.t [@key "targetId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    success : bool;
       [@key "success"]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Always set to true. If an error occurs, the response indicates protocol error."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Closes the target. If the target is a page that gets closed too."]

module Expose_dev_tools_protocol = struct
  let name = "Target.exposeDevToolsProtocol"

  type params = {
    target_id : Cdp_base.Target.Target_id.t; [@key "targetId"]
    binding_name : string option;
       [@key "bindingName"] [@option] [@json.drop_default] [@ocaml.doc "Binding name, 'cdp' if not specified."]
    inherit_permissions : bool option;
       [@key "inheritPermissions"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "If true, inherits the current root session's permissions (default: false)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Inject object to the target's main frame that provides a communication\n\
   channel with browser target.\n\n\
   Injected object will be available as `window\\[bindingName\\]`.\n\n\
   The object has the following API:\n\
   - `binding.send(json)` - a method to send messages over the remote debugging protocol\n\
   - `binding.onmessage = json => handleMessage(json)` - a callback that will be called for the protocol notifications \
   and command responses."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Create_browser_context = struct
  let name = "Target.createBrowserContext"

  type params = {
    dispose_on_detach : bool option;
       [@key "disposeOnDetach"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "If specified, disposes this context when debugging session disconnects."]
    proxy_server : string option;
       [@key "proxyServer"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Proxy server, similar to the one passed to --proxy-server"]
    proxy_bypass_list : string option;
       [@key "proxyBypassList"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Proxy bypass list, similar to the one passed to --proxy-bypass-list"]
    origins_with_universal_network_access : string list option;
       [@key "originsWithUniversalNetworkAccess"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "An optional list of origins to grant unlimited cross-origin access to.\n\
          Parts of the URL other than those constituting origin are ignored."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    browser_context_id : Cdp_base.Browser.Browser_context_id.t;
       [@key "browserContextId"] [@ocaml.doc "The id of the context created."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Creates a new empty BrowserContext. Similar to an incognito profile but you can have more than\none."]

module Get_browser_contexts = struct
  let name = "Target.getBrowserContexts"

  type result = {
    browser_context_ids : Cdp_base.Browser.Browser_context_id.t list;
       [@key "browserContextIds"] [@ocaml.doc "An array of browser context ids."]
    default_browser_context_id : Cdp_base.Browser.Browser_context_id.t option;
       [@key "defaultBrowserContextId"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "The id of the default browser context if available."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Returns all browser contexts created with `Target.createBrowserContext` method."]

module Create_target = struct
  let name = "Target.createTarget"

  type params = {
    url : string;
       [@key "url"] [@ocaml.doc "The initial URL the page will be navigated to. An empty string indicates about:blank."]
    left : int option;
       [@key "left"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Frame left origin in DIP (requires newWindow to be true or headless shell)."]
    top : int option;
       [@key "top"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Frame top origin in DIP (requires newWindow to be true or headless shell)."]
    width : int option;
       [@key "width"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Frame width in DIP (requires newWindow to be true or headless shell)."]
    height : int option;
       [@key "height"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Frame height in DIP (requires newWindow to be true or headless shell)."]
    window_state : window_state option;
       [@key "windowState"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Frame window state (requires newWindow to be true or headless shell).\nDefault is normal."]
    browser_context_id : Cdp_base.Browser.Browser_context_id.t option;
       [@key "browserContextId"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "The browser context to create the page in."]
    enable_begin_frame_control : bool option;
       [@key "enableBeginFrameControl"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Whether BeginFrames for this target will be controlled via DevTools (headless shell only,\n\
          not supported on MacOS yet, false by default)."]
    new_window : bool option;
       [@key "newWindow"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Whether to create a new Window or Tab (false by default, not supported by headless shell)."]
    background : bool option;
       [@key "background"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Whether to create the target in background or foreground (false by default, not supported\n\
          by headless shell)."]
    for_tab : bool option;
       [@key "forTab"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Whether to create the target of type \"tab\"."]
    hidden : bool option;
       [@key "hidden"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Whether to create a hidden target. The hidden target is observable via protocol, but not\n\
          present in the tab UI strip. Cannot be created with `forTab: true`, `newWindow: true` or\n\
          `background: false`. The life-time of the tab is limited to the life-time of the session."]
    focus : bool option;
       [@key "focus"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If specified, determines whether the new target should be focused.\n\
          By default, the focus behavior depends on the `background` parameter:\n\
          - If `background` is false (default) and `focus` is omitted, the new target is focused and the browser \
          window is brought to the foreground.\n\
          - If `background` is false and `focus` is false, the target is opened but the browser window's focus remains \
          unchanged (e.g., if the window was in the background, it stays there).\n\
          - If `background` is true, setting `focus` to true is not supported and will result in an error."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { target_id : Cdp_base.Target.Target_id.t [@key "targetId"] [@ocaml.doc "The id of the page opened."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Creates a new page."]

module Detach_from_target = struct
  let name = "Target.detachFromTarget"

  type params = {
    session_id : Cdp_base.Target.Session_id.t option;
       [@key "sessionId"] [@option] [@json.drop_default] [@ocaml.doc "Session to detach."]
    target_id : Cdp_base.Target.Target_id.t option;
       [@key "targetId"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Deprecated."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Detaches session with given id."]

module Dispose_browser_context = struct
  let name = "Target.disposeBrowserContext"

  type params = { browser_context_id : Cdp_base.Browser.Browser_context_id.t [@key "browserContextId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Deletes a BrowserContext. All the belonging pages will be closed without calling their\nbeforeunload hooks."]

module Get_target_info = struct
  let name = "Target.getTargetInfo"

  type params = { target_id : Cdp_base.Target.Target_id.t option [@key "targetId"] [@option] [@json.drop_default] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { target_info : target_info [@key "targetInfo"] } [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Returns information about a target."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_targets = struct
  let name = "Target.getTargets"

  type params = {
    filter : target_filter option;
       [@key "filter"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Only targets matching filter will be reported. If filter is not specified\n\
          and target discovery is currently enabled, a filter used for target discovery\n\
          is used for consistency."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { target_infos : target_info list [@key "targetInfos"] [@ocaml.doc "The list of targets."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Retrieves a list of available targets."]

module Send_message_to_target = struct
  let name = "Target.sendMessageToTarget"

  type params = {
    message : string; [@key "message"]
    session_id : Cdp_base.Target.Session_id.t option;
       [@key "sessionId"] [@option] [@json.drop_default] [@ocaml.doc "Identifier of the session."]
    target_id : Cdp_base.Target.Target_id.t option;
       [@key "targetId"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Deprecated."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Sends protocol message over session with given id.\n\
   Consider using flat mode instead; see commands attachToTarget, setAutoAttach,\n\
   and crbug.com/991325."]
[@@ocaml.deprecated "deprecated in CDP"]

module Set_auto_attach = struct
  let name = "Target.setAutoAttach"

  type params = {
    auto_attach : bool; [@key "autoAttach"] [@ocaml.doc "Whether to auto-attach to related targets."]
    wait_for_debugger_on_start : bool;
       [@key "waitForDebuggerOnStart"]
       [@ocaml.doc
         "Whether to pause new targets when attaching to them. Use `Runtime.runIfWaitingForDebugger`\n\
          to run paused targets."]
    flatten : bool option;
       [@key "flatten"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Enables \"flat\" access to the session via specifying sessionId attribute in the commands.\n\
          We plan to make this the default, deprecate non-flattened mode,\n\
          and eventually retire it. See crbug.com/991325."]
    filter : target_filter option;
       [@key "filter"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Only targets matching filter will be attached."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Controls whether to automatically attach to new targets which are considered\n\
   to be directly related to this one (for example, iframes or workers).\n\
   When turned on, attaches to all existing related targets as well. When turned off,\n\
   automatically detaches from all currently attached targets.\n\
   This also clears all targets added by `autoAttachRelated` from the list of targets to watch\n\
   for creation of related targets.\n\
   You might want to call this recursively for auto-attached targets to attach\n\
   to all available targets."]

module Auto_attach_related = struct
  let name = "Target.autoAttachRelated"

  type params = {
    target_id : Cdp_base.Target.Target_id.t; [@key "targetId"]
    wait_for_debugger_on_start : bool;
       [@key "waitForDebuggerOnStart"]
       [@ocaml.doc
         "Whether to pause new targets when attaching to them. Use `Runtime.runIfWaitingForDebugger`\n\
          to run paused targets."]
    filter : target_filter option;
       [@key "filter"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Only targets matching filter will be attached."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Adds the specified target to the list of targets that will be monitored for any related target\n\
   creation (such as child frames, child workers and new versions of service worker) and reported\n\
   through `attachedToTarget`. The specified target is also auto-attached.\n\
   This cancels the effect of any previous `setAutoAttach` and is also cancelled by subsequent\n\
   `setAutoAttach`. Only available at the Browser target."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_discover_targets = struct
  let name = "Target.setDiscoverTargets"

  type params = {
    discover : bool; [@key "discover"] [@ocaml.doc "Whether to discover available targets."]
    filter : target_filter option;
       [@key "filter"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Only targets matching filter will be attached. If `discover` is false,\n`filter` must be omitted or empty."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Controls whether to discover available targets and notify via\n\
   `targetCreated/targetInfoChanged/targetDestroyed` events."]

module Set_remote_locations = struct
  let name = "Target.setRemoteLocations"

  type params = { locations : remote_location list [@key "locations"] [@ocaml.doc "List of remote locations."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Enables target discovery for the specified locations, when `setDiscoverTargets` was set to\n`true`."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_dev_tools_target = struct
  let name = "Target.getDevToolsTarget"

  type params = { target_id : Cdp_base.Target.Target_id.t [@key "targetId"] [@ocaml.doc "Page or tab target ID."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    target_id : Cdp_base.Target.Target_id.t option;
       [@key "targetId"] [@option] [@json.drop_default] [@ocaml.doc "The targetId of DevTools page target if exists."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Gets the targetId of the DevTools page target opened for the given target\n(if any)."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Open_dev_tools = struct
  let name = "Target.openDevTools"

  type params = {
    target_id : Cdp_base.Target.Target_id.t; [@key "targetId"] [@ocaml.doc "This can be the page or tab target ID."]
    panel_id : string option;
       [@key "panelId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "The id of the panel we want DevTools to open initially. Currently\n\
          supported panels are elements, console, network, sources, resources,\n\
          timeline, chrome-recorder, heap-profiler, lighthouse, and security."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    target_id : Cdp_base.Target.Target_id.t; [@key "targetId"] [@ocaml.doc "The targetId of DevTools page target."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Opens a DevTools window for the target."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Attached_to_target = struct
  let name = "Target.attachedToTarget"

  type params = {
    session_id : Cdp_base.Target.Session_id.t;
       [@key "sessionId"] [@ocaml.doc "Identifier assigned to the session used to send/receive messages."]
    target_info : target_info; [@key "targetInfo"]
    waiting_for_debugger : bool; [@key "waitingForDebugger"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Issued when attached to target because of auto-attach or `attachToTarget` command."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Detached_from_target = struct
  let name = "Target.detachedFromTarget"

  type params = {
    session_id : Cdp_base.Target.Session_id.t; [@key "sessionId"] [@ocaml.doc "Detached session identifier."]
    target_id : Cdp_base.Target.Target_id.t option;
       [@key "targetId"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Deprecated."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Issued when detached from target for any reason (including `detachFromTarget` command). Can be\n\
   issued multiple times per target if multiple sessions have been attached to it."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Received_message_from_target = struct
  let name = "Target.receivedMessageFromTarget"

  type params = {
    session_id : Cdp_base.Target.Session_id.t;
       [@key "sessionId"] [@ocaml.doc "Identifier of a session which sends a message."]
    message : string; [@key "message"]
    target_id : Cdp_base.Target.Target_id.t option;
       [@key "targetId"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Deprecated."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Notifies about a new protocol message received from the session (as reported in\n`attachedToTarget` event)."]

module Target_created = struct
  let name = "Target.targetCreated"

  type params = { target_info : target_info [@key "targetInfo"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Issued when a possible inspection target is created."]

module Target_destroyed = struct
  let name = "Target.targetDestroyed"

  type params = { target_id : Cdp_base.Target.Target_id.t [@key "targetId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Issued when a target is destroyed."]

module Target_crashed = struct
  let name = "Target.targetCrashed"

  type params = {
    target_id : Cdp_base.Target.Target_id.t; [@key "targetId"]
    status : string; [@key "status"] [@ocaml.doc "Termination status type."]
    error_code : int; [@key "errorCode"] [@ocaml.doc "Termination error code."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Issued when a target has crashed."]

module Target_info_changed = struct
  let name = "Target.targetInfoChanged"

  type params = { target_info : target_info [@key "targetInfo"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Issued when some information about a target has changed. This only happens between\n\
   `targetCreated` and `targetDestroyed`."]
