(* Generated by cdp-gen from Chrome DevTools Protocol r1701330. DO NOT EDIT. *)

[@@@ocaml.doc
"Network domain allows tracking network activities of the page. It exposes information about http,\n\
 file, data and other requests and responses, their headers, bodies, timing, etc."]

include Cdp_network_types
open Jsonkit.Primitives

module Can_clear_browser_cache = struct
  let name = "Network.canClearBrowserCache"

  type result = { result : bool [@key "result"] [@ocaml.doc "True if browser cache can be cleared."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Tells whether clearing browser cache is supported."] [@@ocaml.deprecated "deprecated in CDP"]

module Can_clear_browser_cookies = struct
  let name = "Network.canClearBrowserCookies"

  type result = { result : bool [@key "result"] [@ocaml.doc "True if browser cookies can be cleared."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Tells whether clearing browser cookies is supported."] [@@ocaml.deprecated "deprecated in CDP"]

module Can_emulate_network_conditions = struct
  let name = "Network.canEmulateNetworkConditions"

  type result = { result : bool [@key "result"] [@ocaml.doc "True if emulation of network conditions is supported."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Tells whether emulation of network conditions is supported."] [@@ocaml.deprecated "deprecated in CDP"]

module Clear_browser_cache = struct
  let name = "Network.clearBrowserCache"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Clears browser cache."]

module Clear_browser_cookies = struct
  let name = "Network.clearBrowserCookies"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Clears browser cookies."]

module Delete_cookies = struct
  let name = "Network.deleteCookies"

  type params = {
    name : string; [@key "name"] [@ocaml.doc "Name of the cookies to remove."]
    url : string option;
       [@key "url"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "If specified, deletes all the cookies with the given name where domain and path match\nprovided URL."]
    domain : string option;
       [@key "domain"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "If specified, deletes only cookies with the exact domain."]
    path : string option;
       [@key "path"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "If specified, deletes only cookies with the exact path."]
    partition_key : cookie_partition_key option;
       [@key "partitionKey"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If specified, deletes only cookies with the the given name and partitionKey where\n\
          all partition key attributes match the cookie partition key attribute."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Deletes browser cookies with matching name and url or domain/path/partitionKey pair."]

module Disable = struct
  let name = "Network.disable"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Disables network tracking, prevents network events from being sent to the client."]

module Emulate_network_conditions = struct
  let name = "Network.emulateNetworkConditions"

  type params = {
    offline : bool; [@key "offline"] [@ocaml.doc "True to emulate internet disconnection."]
    latency : Cdp_json.number;
       [@key "latency"] [@ocaml.doc "Minimum latency from request sent to response headers received (ms)."]
    download_throughput : Cdp_json.number;
       [@key "downloadThroughput"]
       [@ocaml.doc "Maximal aggregated download throughput (bytes/sec). -1 disables download throttling."]
    upload_throughput : Cdp_json.number;
       [@key "uploadThroughput"]
       [@ocaml.doc "Maximal aggregated upload throughput (bytes/sec).  -1 disables upload throttling."]
    connection_type : connection_type option;
       [@key "connectionType"] [@option] [@json.drop_default] [@ocaml.doc "Connection type if known."]
    packet_loss : Cdp_json.number option;
       [@key "packetLoss"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "WebRTC packet loss (percent, 0-100). 0 disables packet loss emulation, 100 drops all the packets."]
    packet_queue_length : int option;
       [@key "packetQueueLength"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "WebRTC packet queue length (packet). 0 removes any queue length limitations."]
    packet_reordering : bool option;
       [@key "packetReordering"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "WebRTC packetReordering feature."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Activates emulation of network conditions. This command is deprecated in favor of the emulateNetworkConditionsByRule\n\
   and overrideNetworkState commands, which can be used together to the same effect."]
[@@ocaml.deprecated "deprecated in CDP"]

module Emulate_network_conditions_by_rule = struct
  let name = "Network.emulateNetworkConditionsByRule"

  type params = {
    offline : bool option;
       [@key "offline"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc
         "True to emulate internet disconnection. Deprecated, use the offline property in matchedNetworkConditions\n\
          or emulateOfflineServiceWorker instead."]
    emulate_offline_service_worker : bool option;
       [@key "emulateOfflineServiceWorker"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "True to emulate offline service worker."]
    matched_network_conditions : network_conditions list;
       [@key "matchedNetworkConditions"]
       [@ocaml.doc
         "Configure conditions for matching requests. If multiple entries match a request, the first entry wins.  Global\n\
          conditions can be configured by leaving the urlPattern for the conditions empty. These global conditions are\n\
          also applied for throttling of p2p connections."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    rule_ids : string list;
       [@key "ruleIds"]
       [@ocaml.doc
         "An id for each entry in matchedNetworkConditions. The id will be included in the requestWillBeSentExtraInfo \
          for\n\
          requests affected by a rule."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Activates emulation of network conditions for individual requests using URL match patterns. Unlike the deprecated\n\
   Network.emulateNetworkConditions this method does not affect `navigator` state. Use Network.overrideNetworkState to\n\
   explicitly modify `navigator` behavior."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Override_network_state = struct
  let name = "Network.overrideNetworkState"

  type params = {
    offline : bool; [@key "offline"] [@ocaml.doc "True to emulate internet disconnection."]
    latency : Cdp_json.number;
       [@key "latency"] [@ocaml.doc "Minimum latency from request sent to response headers received (ms)."]
    download_throughput : Cdp_json.number;
       [@key "downloadThroughput"]
       [@ocaml.doc "Maximal aggregated download throughput (bytes/sec). -1 disables download throttling."]
    upload_throughput : Cdp_json.number;
       [@key "uploadThroughput"]
       [@ocaml.doc "Maximal aggregated upload throughput (bytes/sec).  -1 disables upload throttling."]
    connection_type : connection_type option;
       [@key "connectionType"] [@option] [@json.drop_default] [@ocaml.doc "Connection type if known."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Override the state of navigator.onLine and navigator.connection."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Enable = struct
  let name = "Network.enable"

  type params = {
    max_total_buffer_size : int option;
       [@key "maxTotalBufferSize"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Buffer size in bytes to use when preserving network payloads (XHRs, etc).\n\
          This is the maximum number of bytes that will be collected by this\n\
          DevTools session."]
    max_resource_buffer_size : int option;
       [@key "maxResourceBufferSize"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Per-resource buffer size in bytes to use when preserving network payloads (XHRs, etc)."]
    max_post_data_size : int option;
       [@key "maxPostDataSize"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Longest post body size (in bytes) that would be included in requestWillBeSent notification"]
    report_direct_socket_traffic : bool option;
       [@key "reportDirectSocketTraffic"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Whether DirectSocket chunk send/receive events should be reported."]
    enable_durable_messages : bool option;
       [@key "enableDurableMessages"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Enable storing response bodies outside of renderer, so that these survive\n\
          a cross-process navigation. Requires maxTotalBufferSize to be set.\n\
          Currently defaults to false. This field is being deprecated in favor of the dedicated\n\
          configureDurableMessages command, due to the possibility of deadlocks when awaiting\n\
          Network.enable before issuing Runtime.runIfWaitingForDebugger."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Enables network tracking, network events will now be delivered to the client."]

module Configure_durable_messages = struct
  let name = "Network.configureDurableMessages"

  type params = {
    max_total_buffer_size : int option;
       [@key "maxTotalBufferSize"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Buffer size in bytes to use when preserving network payloads (XHRs, etc)."]
    max_resource_buffer_size : int option;
       [@key "maxResourceBufferSize"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Per-resource buffer size in bytes to use when preserving network payloads (XHRs, etc)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Configures storing response bodies outside of renderer, so that these survive\n\
   a cross-process navigation.\n\
   If maxTotalBufferSize is not set, durable messages are disabled."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_all_cookies = struct
  let name = "Network.getAllCookies"

  type result = { cookies : cookie list [@key "cookies"] [@ocaml.doc "Array of cookie objects."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc
  "Returns all browser cookies. Depending on the backend support, will return detailed cookie\n\
   information in the `cookies` field.\n\
   Deprecated. Use Storage.getCookies instead."]
[@@ocaml.deprecated "deprecated in CDP"]

module Get_certificate = struct
  let name = "Network.getCertificate"

  type params = { origin : string [@key "origin"] [@ocaml.doc "Origin to get certificate for."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { table_names : string list [@key "tableNames"] } [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Returns the DER-encoded certificate."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_cookies = struct
  let name = "Network.getCookies"

  type params = {
    urls : string list option;
       [@key "urls"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "The list of URLs for which applicable cookies will be fetched.\n\
          If not specified, it's assumed to be set to the list containing\n\
          the URLs of the page and all of its subframes."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { cookies : cookie list [@key "cookies"] [@ocaml.doc "Array of cookie objects."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Returns all browser cookies for the current URL. Depending on the backend support, will return\n\
   detailed cookie information in the `cookies` field."]

module Get_response_body = struct
  let name = "Network.getResponseBody"

  type params = {
    request_id : Cdp_base.Network.Request_id.t;
       [@key "requestId"] [@ocaml.doc "Identifier of the network request to get content for."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    body : string; [@key "body"] [@ocaml.doc "Response body."]
    base64_encoded : bool; [@key "base64Encoded"] [@ocaml.doc "True, if content was sent as base64."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Returns content served for the given request."]

module Get_request_post_data = struct
  let name = "Network.getRequestPostData"

  type params = {
    request_id : Cdp_base.Network.Request_id.t;
       [@key "requestId"] [@ocaml.doc "Identifier of the network request to get content for."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    post_data : string; [@key "postData"] [@ocaml.doc "Request body string, omitting files from multipart requests"]
    base64_encoded : bool; [@key "base64Encoded"] [@ocaml.doc "True, if content was sent as base64."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Returns post data sent with the request. Returns an error when no data was sent with the request."]

module Replay_xhr = struct
  let name = "Network.replayXHR"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Identifier of XHR to replay."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "This method sends a new XMLHttpRequest which is identical to the original one. The following\n\
   parameters should be identical: method, url, async, request body, extra headers, withCredentials\n\
   attribute, user, password."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Search_in_response_body = struct
  let name = "Network.searchInResponseBody"

  type params = {
    request_id : Cdp_base.Network.Request_id.t;
       [@key "requestId"] [@ocaml.doc "Identifier of the network response to search."]
    query : string; [@key "query"] [@ocaml.doc "String to search for."]
    case_sensitive : bool option;
       [@key "caseSensitive"] [@option] [@json.drop_default] [@ocaml.doc "If true, search is case sensitive."]
    is_regex : bool option;
       [@key "isRegex"] [@option] [@json.drop_default] [@ocaml.doc "If true, treats string parameter as regex."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { result : Cdp_debugger_types.search_match list [@key "result"] [@ocaml.doc "List of search matches."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Searches for given string in response content."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_blocked_ur_ls = struct
  let name = "Network.setBlockedURLs"

  type params = {
    url_patterns : block_pattern list option;
       [@key "urlPatterns"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Patterns to match in the order in which they are given. These patterns\n\
          also take precedence over any wildcard patterns defined in `urls`."]
    urls : string list option;
       [@key "urls"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "URL patterns to block. Wildcards ('*') are allowed."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Blocks URLs from loading."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_bypass_service_worker = struct
  let name = "Network.setBypassServiceWorker"

  type params = { bypass : bool [@key "bypass"] [@ocaml.doc "Bypass service worker and load from network."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Toggles ignoring of service worker for each request."]

module Set_cache_disabled = struct
  let name = "Network.setCacheDisabled"

  type params = { cache_disabled : bool [@key "cacheDisabled"] [@ocaml.doc "Cache disabled state."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Toggles ignoring cache for each request. If `true`, cache will not be used."]

module Set_cookie = struct
  let name = "Network.setCookie"

  type params = {
    name : string; [@key "name"] [@ocaml.doc "Cookie name."]
    value : string; [@key "value"] [@ocaml.doc "Cookie value."]
    url : string option;
       [@key "url"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "The request-URI to associate with the setting of the cookie. This value can affect the\n\
          default domain, path, source port, and source scheme values of the created cookie."]
    domain : string option; [@key "domain"] [@option] [@json.drop_default] [@ocaml.doc "Cookie domain."]
    path : string option; [@key "path"] [@option] [@json.drop_default] [@ocaml.doc "Cookie path."]
    secure : bool option; [@key "secure"] [@option] [@json.drop_default] [@ocaml.doc "True if cookie is secure."]
    http_only : bool option;
       [@key "httpOnly"] [@option] [@json.drop_default] [@ocaml.doc "True if cookie is http-only."]
    same_site : cookie_same_site option;
       [@key "sameSite"] [@option] [@json.drop_default] [@ocaml.doc "Cookie SameSite type."]
    expires : Cdp_base.Network.Time_since_epoch.t option;
       [@key "expires"] [@option] [@json.drop_default] [@ocaml.doc "Cookie expiration date, session cookie if not set"]
    priority : cookie_priority option;
       [@key "priority"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Cookie Priority type."]
    source_scheme : cookie_source_scheme option;
       [@key "sourceScheme"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Cookie source scheme type."]
    source_port : int option;
       [@key "sourcePort"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Cookie source port. Valid values are \\{-1, \\[1, 65535\\]\\}, -1 indicates an unspecified port.\n\
          An unspecified port value allows protocol clients to emulate legacy cookie scope for the port.\n\
          This is a temporary ability and it will be removed in the future."]
    partition_key : cookie_partition_key option;
       [@key "partitionKey"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Cookie partition key. If not set, the cookie will be set as not partitioned."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    success : bool;
       [@key "success"]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Always set to true. If an error occurs, the response indicates protocol error."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Sets a cookie with the given cookie data; may overwrite equivalent cookies if they exist."]

module Set_cookies = struct
  let name = "Network.setCookies"

  type params = { cookies : cookie_param list [@key "cookies"] [@ocaml.doc "Cookies to be set."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Sets given cookies."]

module Set_extra_http_headers = struct
  let name = "Network.setExtraHTTPHeaders"

  type params = { headers : headers [@key "headers"] [@ocaml.doc "Map with extra HTTP headers."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Specifies whether to always send extra HTTP headers with the requests from this page."]

module Set_attach_debug_stack = struct
  let name = "Network.setAttachDebugStack"

  type params = {
    enabled : bool; [@key "enabled"] [@ocaml.doc "Whether to attach a page script stack for debugging purpose."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Specifies whether to attach a page script stack id in requests"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_user_agent_override = struct
  let name = "Network.setUserAgentOverride"

  type params = {
    user_agent : string; [@key "userAgent"] [@ocaml.doc "User agent to use."]
    accept_language : string option;
       [@key "acceptLanguage"] [@option] [@json.drop_default] [@ocaml.doc "Browser language to emulate."]
    platform : string option;
       [@key "platform"] [@option] [@json.drop_default] [@ocaml.doc "The platform navigator.platform should return."]
    user_agent_metadata : Cdp_emulation_types.user_agent_metadata option;
       [@key "userAgentMetadata"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "To be sent in Sec-CH-UA-* headers and returned in navigator.userAgentData"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Allows overriding user agent with the given string."]
[@@alert redirected "redirected to the Emulation domain in CDP"]

module Stream_resource_content = struct
  let name = "Network.streamResourceContent"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Identifier of the request to stream."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    buffered_data : string;
       [@key "bufferedData"]
       [@ocaml.doc
         "Data that has been buffered until streaming is enabled. (Encoded as a base64 string when passed over JSON)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Enables streaming of the response for the given requestId.\n\
   If enabled, the dataReceived event contains the data that was received during streaming."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_security_isolation_status = struct
  let name = "Network.getSecurityIsolationStatus"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t option;
       [@key "frameId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "If no frameId is provided, the status of the target is provided."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { status : security_isolation_status [@key "status"] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Returns information about the COEP/COOP isolation status."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Enable_reporting_api = struct
  let name = "Network.enableReportingApi"

  type params = {
    enable : bool; [@key "enable"] [@ocaml.doc "Whether to enable or disable events for the Reporting API"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Enables tracking for the Reporting API, events generated by the Reporting API will now be delivered to the client.\n\
   Enabling triggers 'reportingApiReportAdded' for all existing reports."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Enable_device_bound_sessions = struct
  let name = "Network.enableDeviceBoundSessions"

  type params = { enable : bool [@key "enable"] [@ocaml.doc "Whether to enable or disable events."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Sets up tracking device bound sessions and fetching of initial set of sessions."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Delete_device_bound_session = struct
  let name = "Network.deleteDeviceBoundSession"

  type params = { key : device_bound_session_key [@key "key"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Deletes a device bound session."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Fetch_schemeful_site = struct
  let name = "Network.fetchSchemefulSite"

  type params = { origin : string [@key "origin"] [@ocaml.doc "The URL origin."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { schemeful_site : string [@key "schemefulSite"] [@ocaml.doc "The corresponding schemeful site."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Fetches the schemeful site for a specific origin."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Load_network_resource = struct
  let name = "Network.loadNetworkResource"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t option;
       [@key "frameId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Frame id to get the resource for. Mandatory for frame targets, and\nshould be omitted for worker targets."]
    url : string; [@key "url"] [@ocaml.doc "URL of the resource to get content for."]
    options : load_network_resource_options; [@key "options"] [@ocaml.doc "Options for the request."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { resource : load_network_resource_page_result [@key "resource"] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Fetches the resource and returns the content."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_cookie_controls = struct
  let name = "Network.setCookieControls"

  type params = {
    enable_third_party_cookie_restriction : bool;
       [@key "enableThirdPartyCookieRestriction"] [@ocaml.doc "Whether 3pc restriction is enabled."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Sets Controls for third-party cookie access\nPage reload is required before the new cookie behavior will be observed"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Data_received = struct
  let name = "Network.dataReceived"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    data_length : int; [@key "dataLength"] [@ocaml.doc "Data chunk length."]
    encoded_data_length : int;
       [@key "encodedDataLength"]
       [@ocaml.doc "Actual bytes received (might be less than dataLength for compressed encodings)."]
    data : string option;
       [@key "data"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Data that was received. (Encoded as a base64 string when passed over JSON)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when data chunk was received over the network."]

module Event_source_message_received = struct
  let name = "Network.eventSourceMessageReceived"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    event_name : string; [@key "eventName"] [@ocaml.doc "Message type."]
    event_id : string; [@key "eventId"] [@ocaml.doc "Message identifier."]
    data : string; [@key "data"] [@ocaml.doc "Message content."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when EventSource message is received."]

module Loading_failed = struct
  let name = "Network.loadingFailed"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    type_ : resource_type; [@key "type"] [@ocaml.doc "Resource type."]
    error_text : string;
       [@key "errorText"]
       [@ocaml.doc
         "Error message. List of network errors: https://cs.chromium.org/chromium/src/net/base/net_error_list.h"]
    canceled : bool option;
       [@key "canceled"] [@option] [@json.drop_default] [@ocaml.doc "True if loading was canceled."]
    blocked_reason : blocked_reason option;
       [@key "blockedReason"] [@option] [@json.drop_default] [@ocaml.doc "The reason why loading was blocked, if any."]
    cors_error_status : cors_error_status option;
       [@key "corsErrorStatus"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The reason why loading was blocked by CORS, if any."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when HTTP request has failed to load."]

module Loading_finished = struct
  let name = "Network.loadingFinished"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    encoded_data_length : Cdp_json.number;
       [@key "encodedDataLength"] [@ocaml.doc "Total number of bytes received for this request."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when HTTP request has finished loading."]

module Request_served_from_cache = struct
  let name = "Network.requestServedFromCache"

  type params = { request_id : Cdp_base.Network.Request_id.t [@key "requestId"] [@ocaml.doc "Request identifier."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired if request ended up loading from cache."]

module Request_will_be_sent = struct
  let name = "Network.requestWillBeSent"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    loader_id : Cdp_base.Network.Loader_id.t;
       [@key "loaderId"] [@ocaml.doc "Loader identifier. Empty string if the request is fetched from worker."]
    document_url : string; [@key "documentURL"] [@ocaml.doc "URL of the document this request is loaded for."]
    request : request; [@key "request"] [@ocaml.doc "Request data."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    wall_time : Cdp_base.Network.Time_since_epoch.t; [@key "wallTime"] [@ocaml.doc "Timestamp."]
    initiator : initiator; [@key "initiator"] [@ocaml.doc "Request initiator."]
    redirect_has_extra_info : bool;
       [@key "redirectHasExtraInfo"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "In the case that redirectResponse is populated, this flag indicates whether\n\
          requestWillBeSentExtraInfo and responseReceivedExtraInfo events will be or were emitted\n\
          for the request which was just redirected."]
    redirect_response : response option;
       [@key "redirectResponse"] [@option] [@json.drop_default] [@ocaml.doc "Redirect response data."]
    type_ : resource_type option; [@key "type"] [@option] [@json.drop_default] [@ocaml.doc "Type of this resource."]
    frame_id : Cdp_base.Page.Frame_id.t option;
       [@key "frameId"] [@option] [@json.drop_default] [@ocaml.doc "Frame identifier."]
    has_user_gesture : bool option;
       [@key "hasUserGesture"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Whether the request is initiated by a user gesture. Defaults to false."]
    render_blocking_behavior : render_blocking_behavior option;
       [@key "renderBlockingBehavior"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "The render-blocking behavior of the request."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when page is about to send HTTP request."]

module Resource_changed_priority = struct
  let name = "Network.resourceChangedPriority"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    new_priority : resource_priority; [@key "newPriority"] [@ocaml.doc "New priority"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when resource loading priority is changed"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Signed_exchange_received = struct
  let name = "Network.signedExchangeReceived"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    info : signed_exchange_info; [@key "info"] [@ocaml.doc "Information about the signed exchange response."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when a signed exchange was received over the network"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Response_received = struct
  let name = "Network.responseReceived"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    loader_id : Cdp_base.Network.Loader_id.t;
       [@key "loaderId"] [@ocaml.doc "Loader identifier. Empty string if the request is fetched from worker."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    type_ : resource_type; [@key "type"] [@ocaml.doc "Resource type."]
    response : response; [@key "response"] [@ocaml.doc "Response data."]
    has_extra_info : bool;
       [@key "hasExtraInfo"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Indicates whether requestWillBeSentExtraInfo and responseReceivedExtraInfo events will be\n\
          or were emitted for this request."]
    frame_id : Cdp_base.Page.Frame_id.t option;
       [@key "frameId"] [@option] [@json.drop_default] [@ocaml.doc "Frame identifier."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when HTTP response is available."]

module Web_socket_closed = struct
  let name = "Network.webSocketClosed"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when WebSocket is closed."]

module Web_socket_created = struct
  let name = "Network.webSocketCreated"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    url : string; [@key "url"] [@ocaml.doc "WebSocket request URL."]
    initiator : initiator option; [@key "initiator"] [@option] [@json.drop_default] [@ocaml.doc "Request initiator."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired upon WebSocket creation."]

module Web_socket_frame_error = struct
  let name = "Network.webSocketFrameError"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    error_message : string; [@key "errorMessage"] [@ocaml.doc "WebSocket error message."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when WebSocket message error occurs."]

module Web_socket_frame_received = struct
  let name = "Network.webSocketFrameReceived"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    response : web_socket_frame; [@key "response"] [@ocaml.doc "WebSocket response data."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when WebSocket message is received."]

module Web_socket_frame_sent = struct
  let name = "Network.webSocketFrameSent"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    response : web_socket_frame; [@key "response"] [@ocaml.doc "WebSocket response data."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when WebSocket message is sent."]

module Web_socket_handshake_response_received = struct
  let name = "Network.webSocketHandshakeResponseReceived"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    response : web_socket_response; [@key "response"] [@ocaml.doc "WebSocket response data."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when WebSocket handshake response becomes available."]

module Web_socket_will_send_handshake_request = struct
  let name = "Network.webSocketWillSendHandshakeRequest"

  type params = {
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"] [@ocaml.doc "Request identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    wall_time : Cdp_base.Network.Time_since_epoch.t; [@key "wallTime"] [@ocaml.doc "UTC Timestamp."]
    request : web_socket_request; [@key "request"] [@ocaml.doc "WebSocket request data."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when WebSocket is about to initiate handshake."]

module Web_transport_created = struct
  let name = "Network.webTransportCreated"

  type params = {
    transport_id : Cdp_base.Network.Request_id.t; [@key "transportId"] [@ocaml.doc "WebTransport identifier."]
    url : string; [@key "url"] [@ocaml.doc "WebTransport request URL."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
    initiator : initiator option; [@key "initiator"] [@option] [@json.drop_default] [@ocaml.doc "Request initiator."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired upon WebTransport creation."]

module Web_transport_connection_established = struct
  let name = "Network.webTransportConnectionEstablished"

  type params = {
    transport_id : Cdp_base.Network.Request_id.t; [@key "transportId"] [@ocaml.doc "WebTransport identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when WebTransport handshake is finished."]

module Web_transport_closed = struct
  let name = "Network.webTransportClosed"

  type params = {
    transport_id : Cdp_base.Network.Request_id.t; [@key "transportId"] [@ocaml.doc "WebTransport identifier."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"] [@ocaml.doc "Timestamp."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when WebTransport is disposed."]

module Direct_tcp_socket_created = struct
  let name = "Network.directTCPSocketCreated"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    remote_addr : string; [@key "remoteAddr"]
    remote_port : int; [@key "remotePort"] [@ocaml.doc "Unsigned int 16."]
    options : direct_tcp_socket_options; [@key "options"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
    initiator : initiator option; [@key "initiator"] [@option] [@json.drop_default]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired upon direct_socket.TCPSocket creation."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_tcp_socket_opened = struct
  let name = "Network.directTCPSocketOpened"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    remote_addr : string; [@key "remoteAddr"]
    remote_port : int; [@key "remotePort"] [@ocaml.doc "Expected to be unsigned integer."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
    local_addr : string option; [@key "localAddr"] [@option] [@json.drop_default]
    local_port : int option;
       [@key "localPort"] [@option] [@json.drop_default] [@ocaml.doc "Expected to be unsigned integer."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when direct_socket.TCPSocket connection is opened."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_tcp_socket_aborted = struct
  let name = "Network.directTCPSocketAborted"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    error_message : error_reason; [@key "errorMessage"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when direct_socket.TCPSocket is aborted."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_tcp_socket_closed = struct
  let name = "Network.directTCPSocketClosed"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when direct_socket.TCPSocket is closed."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_tcp_socket_chunk_sent = struct
  let name = "Network.directTCPSocketChunkSent"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    data : string; [@key "data"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when data is sent to tcp direct socket stream."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_tcp_socket_chunk_received = struct
  let name = "Network.directTCPSocketChunkReceived"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    data : string; [@key "data"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when data is received from tcp direct socket stream."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_udp_socket_joined_multicast_group = struct
  let name = "Network.directUDPSocketJoinedMulticastGroup"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    ip_address : string; [@key "IPAddress"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_udp_socket_left_multicast_group = struct
  let name = "Network.directUDPSocketLeftMulticastGroup"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    ip_address : string; [@key "IPAddress"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_udp_socket_created = struct
  let name = "Network.directUDPSocketCreated"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    options : direct_udp_socket_options; [@key "options"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
    initiator : initiator option; [@key "initiator"] [@option] [@json.drop_default]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired upon direct_socket.UDPSocket creation."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_udp_socket_opened = struct
  let name = "Network.directUDPSocketOpened"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    local_addr : string; [@key "localAddr"]
    local_port : int; [@key "localPort"] [@ocaml.doc "Expected to be unsigned integer."]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
    remote_addr : string option; [@key "remoteAddr"] [@option] [@json.drop_default]
    remote_port : int option;
       [@key "remotePort"] [@option] [@json.drop_default] [@ocaml.doc "Expected to be unsigned integer."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when direct_socket.UDPSocket connection is opened."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_udp_socket_aborted = struct
  let name = "Network.directUDPSocketAborted"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    error_message : error_reason; [@key "errorMessage"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when direct_socket.UDPSocket is aborted."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_udp_socket_closed = struct
  let name = "Network.directUDPSocketClosed"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when direct_socket.UDPSocket is closed."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_udp_socket_chunk_sent = struct
  let name = "Network.directUDPSocketChunkSent"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    message : direct_udp_message; [@key "message"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when message is sent to udp direct socket stream."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Direct_udp_socket_chunk_received = struct
  let name = "Network.directUDPSocketChunkReceived"

  type params = {
    identifier : Cdp_base.Network.Request_id.t; [@key "identifier"]
    message : direct_udp_message; [@key "message"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when message is received from udp direct socket stream."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Request_will_be_sent_extra_info = struct
  let name = "Network.requestWillBeSentExtraInfo"

  type params = {
    request_id : Cdp_base.Network.Request_id.t;
       [@key "requestId"]
       [@ocaml.doc "Request identifier. Used to match this information to an existing requestWillBeSent event."]
    associated_cookies : associated_cookie list;
       [@key "associatedCookies"]
       [@ocaml.doc
         "A list of cookies potentially associated to the requested URL. This includes both cookies sent with\n\
          the request and the ones not sent; the latter are distinguished by having blockedReasons field set."]
    headers : headers; [@key "headers"] [@ocaml.doc "Raw request headers as they will be sent over the wire."]
    connect_timing : connect_timing;
       [@key "connectTiming"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Connection timing information for the request."]
    device_bound_session_usages : device_bound_session_with_usage list option;
       [@key "deviceBoundSessionUsages"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "How the request site's device bound sessions were used during this request."]
    client_security_state : client_security_state option;
       [@key "clientSecurityState"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The client security state set for the request."]
    site_has_cookie_in_other_partition : bool option;
       [@key "siteHasCookieInOtherPartition"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Whether the site has partitioned cookies stored in a partition different than the current one."]
    applied_network_conditions_id : string option;
       [@key "appliedNetworkConditionsId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "The network conditions id if this request was affected by network conditions configured via\n\
          emulateNetworkConditionsByRule."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired when additional information about a requestWillBeSent event is available from the\n\
   network stack. Not every requestWillBeSent event will have an additional\n\
   requestWillBeSentExtraInfo fired for it, and there is no guarantee whether requestWillBeSent\n\
   or requestWillBeSentExtraInfo will be fired first for the same request."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Response_received_extra_info = struct
  let name = "Network.responseReceivedExtraInfo"

  type params = {
    request_id : Cdp_base.Network.Request_id.t;
       [@key "requestId"]
       [@ocaml.doc "Request identifier. Used to match this information to another responseReceived event."]
    blocked_cookies : blocked_set_cookie_with_reason list;
       [@key "blockedCookies"]
       [@ocaml.doc
         "A list of cookies which were not stored from the response along with the corresponding\n\
          reasons for blocking. The cookies here may not be valid due to syntax errors, which\n\
          are represented by the invalid cookie line string instead of a proper cookie."]
    headers : headers;
       [@key "headers"]
       [@ocaml.doc
         "Raw response headers as they were received over the wire.\n\
          Duplicate headers in the response are represented as a single key with their values\n\
          concatentated using `\\\\n` as the separator.\n\
          See also `headersText` that contains verbatim text for HTTP/1.*."]
    resource_ip_address_space : ip_address_space;
       [@key "resourceIPAddressSpace"]
       [@ocaml.doc
         "The IP address space of the resource. The address space can only be determined once the transport\n\
          established the connection, so we can't send it in `requestWillBeSentExtraInfo`."]
    status_code : int;
       [@key "statusCode"]
       [@ocaml.doc
         "The status code of the response. This is useful in cases the request failed and no responseReceived\n\
          event is triggered, which is the case for, e.g., CORS errors. This is also the correct status code\n\
          for cached requests, where the status in responseReceived is a 200 and this will be 304."]
    headers_text : string option;
       [@key "headersText"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Raw response header text as it was received over the wire. The raw text may not always be\n\
          available, such as in the case of HTTP/2 or QUIC."]
    cookie_partition_key : cookie_partition_key option;
       [@key "cookiePartitionKey"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "The cookie partition key that will be used to store partitioned cookies set in this response.\n\
          Only sent when partitioned cookies are enabled."]
    cookie_partition_key_opaque : bool option;
       [@key "cookiePartitionKeyOpaque"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "True if partitioned cookies are enabled, but the partition key is not serializable to string."]
    exempted_cookies : exempted_set_cookie_with_reason list option;
       [@key "exemptedCookies"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "A list of cookies which should have been blocked by 3PCD but are exempted and stored from\n\
          the response with the corresponding reason."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired when additional information about a responseReceived event is available from the network\n\
   stack. Not every responseReceived event will have an additional responseReceivedExtraInfo for\n\
   it, and responseReceivedExtraInfo may be fired before or after responseReceived."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Response_received_early_hints = struct
  let name = "Network.responseReceivedEarlyHints"

  type params = {
    request_id : Cdp_base.Network.Request_id.t;
       [@key "requestId"]
       [@ocaml.doc "Request identifier. Used to match this information to another responseReceived event."]
    headers : headers;
       [@key "headers"]
       [@ocaml.doc
         "Raw response headers as they were received over the wire.\n\
          Duplicate headers in the response are represented as a single key with their values\n\
          concatentated using `\\\\n` as the separator.\n\
          See also `headersText` that contains verbatim text for HTTP/1.*."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired when 103 Early Hints headers is received in addition to the common response.\n\
   Not every responseReceived event will have an responseReceivedEarlyHints fired.\n\
   Only one responseReceivedEarlyHints may be fired for eached responseReceived event."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Trust_token_operation_done = struct
  let name = "Network.trustTokenOperationDone"

  type status =
    | Ok_ [@json.name "Ok"]
    | InvalidArgument [@json.name "InvalidArgument"]
    | MissingIssuerKeys [@json.name "MissingIssuerKeys"]
    | FailedPrecondition [@json.name "FailedPrecondition"]
    | ResourceExhausted [@json.name "ResourceExhausted"]
    | AlreadyExists [@json.name "AlreadyExists"]
    | ResourceLimited [@json.name "ResourceLimited"]
    | Unauthorized [@json.name "Unauthorized"]
    | BadResponse [@json.name "BadResponse"]
    | InternalError [@json.name "InternalError"]
    | UnknownError [@json.name "UnknownError"]
    | FulfilledLocally [@json.name "FulfilledLocally"]
    | SiteIssuerLimit [@json.name "SiteIssuerLimit"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    status : status;
       [@key "status"]
       [@ocaml.doc
         "Detailed success or error status of the operation.\n\
          'AlreadyExists' also signifies a successful operation, as the result\n\
          of the operation already exists und thus, the operation was abort\n\
          preemptively (e.g. a cache hit)."]
    type_ : trust_token_operation_type; [@key "type"]
    request_id : Cdp_base.Network.Request_id.t; [@key "requestId"]
    top_level_origin : string option;
       [@key "topLevelOrigin"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Top level origin. The context in which the operation was attempted."]
    issuer_origin : string option;
       [@key "issuerOrigin"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Origin of the issuer in case of a \"Issuance\" or \"Redemption\" operation."]
    issued_token_count : int option;
       [@key "issuedTokenCount"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The number of obtained Trust Tokens on a successful \"Issuance\" operation."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired exactly once for each Trust Token operation. Depending on\n\
   the type of the operation and whether the operation succeeded or\n\
   failed, the event is fired before the corresponding request was sent\n\
   or after the response was received."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Policy_updated = struct
  let name = "Network.policyUpdated"

  type params = unit [@@deriving show, eq]

  let params_of_json (_ignored_payload : Cdp_json.t) : params = ()

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired once security policy has been updated."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Reporting_api_report_added = struct
  let name = "Network.reportingApiReportAdded"

  type params = { report : reporting_api_report [@key "report"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Is sent whenever a new report is added.\nAnd after 'enableReportingApi' for all existing reports."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Reporting_api_report_updated = struct
  let name = "Network.reportingApiReportUpdated"

  type params = { report : reporting_api_report [@key "report"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Reporting_api_endpoints_changed_for_origin = struct
  let name = "Network.reportingApiEndpointsChangedForOrigin"

  type params = {
    origin : string; [@key "origin"] [@ocaml.doc "Origin of the document(s) which configured the endpoints."]
    endpoints : reporting_api_endpoint list; [@key "endpoints"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Device_bound_sessions_added = struct
  let name = "Network.deviceBoundSessionsAdded"

  type params = { sessions : device_bound_session list [@key "sessions"] [@ocaml.doc "The device bound sessions."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Triggered when the initial set of device bound sessions is added."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Device_bound_session_event_occurred = struct
  let name = "Network.deviceBoundSessionEventOccurred"

  type params = {
    event_id : Cdp_base.Network.Device_bound_session_event_id.t;
       [@key "eventId"] [@ocaml.doc "A unique identifier for this session event."]
    site : string; [@key "site"] [@ocaml.doc "The site this session event is associated with."]
    succeeded : bool; [@key "succeeded"] [@ocaml.doc "Whether this event was considered successful."]
    session_id : string option;
       [@key "sessionId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The session ID this event is associated with. May not be populated for\nfailed events."]
    creation_event_details : creation_event_details option;
       [@key "creationEventDetails"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The below are the different session event type details. Exactly one is populated."]
    refresh_event_details : refresh_event_details option; [@key "refreshEventDetails"] [@option] [@json.drop_default]
    termination_event_details : termination_event_details option;
       [@key "terminationEventDetails"] [@option] [@json.drop_default]
    challenge_event_details : challenge_event_details option;
       [@key "challengeEventDetails"] [@option] [@json.drop_default]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Triggered when a device bound session event occurs."]
[@@alert experimental "experimental in CDP, may change with Chrome"]
