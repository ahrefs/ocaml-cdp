(* Generated by cdp-gen from Chrome DevTools Protocol r1701330. DO NOT EDIT. *)

[@@@ocaml.doc
"Debugger domain exposes JavaScript debugging capabilities. It allows setting and removing\n\
 breakpoints, stepping through execution, exploring stack traces, etc."]

include Cdp_debugger_types
open Jsonkit.Primitives

module Continue_to_location = struct
  let name = "Debugger.continueToLocation"

  type target_call_frames =
    | Any [@json.name "any"]
    | Current [@json.name "current"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    location : location; [@key "location"] [@ocaml.doc "Location to continue to."]
    target_call_frames : target_call_frames option; [@key "targetCallFrames"] [@option] [@json.drop_default]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Continues execution until specific location is reached."]

module Disable = struct
  let name = "Debugger.disable"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Disables debugger for given page."]

module Enable = struct
  let name = "Debugger.enable"

  type params = {
    max_scripts_cache_size : Cdp_json.number option;
       [@key "maxScriptsCacheSize"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "The maximum size in bytes of collected scripts (not referenced by other heap objects)\n\
          the debugger can hold. Puts no limit if parameter is omitted."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    debugger_id : Cdp_base.Runtime.Unique_debugger_id.t;
       [@key "debuggerId"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Unique identifier of the debugger."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Enables debugger for the given page. Clients should not assume that the debugging has been\n\
   enabled until the result for this command is received."]

module Evaluate_on_call_frame = struct
  let name = "Debugger.evaluateOnCallFrame"

  type params = {
    call_frame_id : Cdp_base.Debugger.Call_frame_id.t;
       [@key "callFrameId"] [@ocaml.doc "Call frame identifier to evaluate on."]
    expression : string; [@key "expression"] [@ocaml.doc "Expression to evaluate."]
    object_group : string option;
       [@key "objectGroup"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "String object group name to put result into (allows rapid releasing resulting object handles\n\
          using `releaseObjectGroup`)."]
    include_command_line_api : bool option;
       [@key "includeCommandLineAPI"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Specifies whether command line API should be available to the evaluated expression, defaults\nto false."]
    silent : bool option;
       [@key "silent"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "In silent mode exceptions thrown during evaluation are not reported and do not pause\n\
          execution. Overrides `setPauseOnException` state."]
    return_by_value : bool option;
       [@key "returnByValue"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Whether the result is expected to be a JSON object that should be sent by value."]
    generate_preview : bool option;
       [@key "generatePreview"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Whether preview should be generated for the result."]
    throw_on_side_effect : bool option;
       [@key "throwOnSideEffect"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Whether to throw an exception if side effect cannot be ruled out during evaluation."]
    timeout : Cdp_base.Runtime.Time_delta.t option;
       [@key "timeout"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Terminate execution after timing out (number of milliseconds)."]
    scope_number : int option;
       [@key "scopeNumber"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Specifies the scope number to evaluate the expression in (default: 0, innermost scope)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    result : Cdp_runtime_types.remote_object; [@key "result"] [@ocaml.doc "Object wrapper for the evaluation result."]
    exception_details : Cdp_runtime_types.exception_details option;
       [@key "exceptionDetails"] [@option] [@json.drop_default] [@ocaml.doc "Exception details."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Evaluates expression on a given call frame."]

module Get_possible_breakpoints = struct
  let name = "Debugger.getPossibleBreakpoints"

  type params = {
    start : location; [@key "start"] [@ocaml.doc "Start of range to search possible breakpoint locations in."]
    end_ : location option;
       [@key "end"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "End of range to search possible breakpoint locations in (excluding). When not specified, end\n\
          of scripts is used as end of range."]
    restrict_to_function : bool option;
       [@key "restrictToFunction"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Only consider locations which are in the same (non-nested) function as start."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    locations : break_location list; [@key "locations"] [@ocaml.doc "List of the possible breakpoint locations."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Returns possible locations for breakpoint. scriptId in start and end range locations should be\nthe same."]

module Get_script_source = struct
  let name = "Debugger.getScriptSource"

  type params = {
    script_id : Cdp_base.Runtime.Script_id.t; [@key "scriptId"] [@ocaml.doc "Id of the script to get source for."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    script_source : string; [@key "scriptSource"] [@ocaml.doc "Script source (empty in case of Wasm bytecode)."]
    bytecode : string option;
       [@key "bytecode"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Wasm bytecode. (Encoded as a base64 string when passed over JSON)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Returns source for the script with given id."]

module Disassemble_wasm_module = struct
  let name = "Debugger.disassembleWasmModule"

  type params = {
    script_id : Cdp_base.Runtime.Script_id.t; [@key "scriptId"] [@ocaml.doc "Id of the script to disassemble"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    stream_id : string option;
       [@key "streamId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "For large modules, return a stream from which additional chunks of\ndisassembly can be read successively."]
    total_number_of_lines : int;
       [@key "totalNumberOfLines"] [@ocaml.doc "The total number of lines in the disassembly text."]
    function_body_offsets : int list;
       [@key "functionBodyOffsets"]
       [@ocaml.doc
         "The offsets of all function bodies, in the format \\[start1, end1,\n\
          start2, end2, ...\\] where all ends are exclusive."]
    chunk : wasm_disassembly_chunk; [@key "chunk"] [@ocaml.doc "The first chunk of disassembly."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Next_wasm_disassembly_chunk = struct
  let name = "Debugger.nextWasmDisassemblyChunk"

  type params = { stream_id : string [@key "streamId"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { chunk : wasm_disassembly_chunk [@key "chunk"] [@ocaml.doc "The next chunk of disassembly."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Disassemble the next chunk of lines for the module corresponding to the\n\
   stream. If disassembly is complete, this API will invalidate the streamId\n\
   and return an empty chunk. Any subsequent calls for the now invalid stream\n\
   will return errors."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_wasm_bytecode = struct
  let name = "Debugger.getWasmBytecode"

  type params = {
    script_id : Cdp_base.Runtime.Script_id.t; [@key "scriptId"] [@ocaml.doc "Id of the Wasm script to get source for."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    bytecode : string; [@key "bytecode"] [@ocaml.doc "Script source. (Encoded as a base64 string when passed over JSON)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "This command is deprecated. Use getScriptSource instead."] [@@ocaml.deprecated "deprecated in CDP"]

module Get_stack_trace = struct
  let name = "Debugger.getStackTrace"

  type params = { stack_trace_id : Cdp_runtime_types.stack_trace_id [@key "stackTraceId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { stack_trace : Cdp_runtime_types.stack_trace [@key "stackTrace"] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Returns stack trace with given `stackTraceId`."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Pause = struct
  let name = "Debugger.pause"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Stops on the next JavaScript statement."]

module Pause_on_async_call = struct
  let name = "Debugger.pauseOnAsyncCall"

  type params = {
    parent_stack_trace_id : Cdp_runtime_types.stack_trace_id;
       [@key "parentStackTraceId"] [@ocaml.doc "Debugger will pause when async call with given stack trace is started."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.deprecated "deprecated in CDP"] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Remove_breakpoint = struct
  let name = "Debugger.removeBreakpoint"

  type params = { breakpoint_id : Cdp_base.Debugger.Breakpoint_id.t [@key "breakpointId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Removes JavaScript breakpoint."]

module Restart_frame = struct
  let name = "Debugger.restartFrame"

  type mode =
    | StepInto [@json.name "StepInto"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    call_frame_id : Cdp_base.Debugger.Call_frame_id.t;
       [@key "callFrameId"] [@ocaml.doc "Call frame identifier to evaluate on."]
    mode : mode option;
       [@key "mode"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "The `mode` parameter must be present and set to 'StepInto', otherwise\n`restartFrame` will error out."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    call_frames : call_frame list;
       [@key "callFrames"] [@ocaml.deprecated "deprecated in CDP"] [@ocaml.doc "New stack trace."]
    async_stack_trace : Cdp_runtime_types.stack_trace option;
       [@key "asyncStackTrace"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Async stack trace, if any."]
    async_stack_trace_id : Cdp_runtime_types.stack_trace_id option;
       [@key "asyncStackTraceId"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Async stack trace, if any."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Restarts particular call frame from the beginning. The old, deprecated\n\
   behavior of `restartFrame` is to stay paused and allow further CDP commands\n\
   after a restart was scheduled. This can cause problems with restarting, so\n\
   we now continue execution immediatly after it has been scheduled until we\n\
   reach the beginning of the restarted frame.\n\n\
   To stay back-wards compatible, `restartFrame` now expects a `mode`\n\
   parameter to be present. If the `mode` parameter is missing, `restartFrame`\n\
   errors out.\n\n\
   The various return values are deprecated and `callFrames` is always empty.\n\
   Use the call frames from the `Debugger#paused` events instead, that fires\n\
   once V8 pauses at the beginning of the restarted function."]

module Resume = struct
  let name = "Debugger.resume"

  type params = {
    terminate_on_resume : bool option;
       [@key "terminateOnResume"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Set to true to terminate execution upon resuming execution. In contrast\n\
          to Runtime.terminateExecution, this will allows to execute further\n\
          JavaScript (i.e. via evaluation) until execution of the paused code\n\
          is actually resumed, at which point termination is triggered.\n\
          If execution is currently not paused, this parameter has no effect."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Resumes JavaScript execution."]

module Search_in_content = struct
  let name = "Debugger.searchInContent"

  type params = {
    script_id : Cdp_base.Runtime.Script_id.t; [@key "scriptId"] [@ocaml.doc "Id of the script to search in."]
    query : string; [@key "query"] [@ocaml.doc "String to search for."]
    case_sensitive : bool option;
       [@key "caseSensitive"] [@option] [@json.drop_default] [@ocaml.doc "If true, search is case sensitive."]
    is_regex : bool option;
       [@key "isRegex"] [@option] [@json.drop_default] [@ocaml.doc "If true, treats string parameter as regex."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { result : search_match list [@key "result"] [@ocaml.doc "List of search matches."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Searches for given string in script content."]

module Set_async_call_stack_depth = struct
  let name = "Debugger.setAsyncCallStackDepth"

  type params = {
    max_depth : int;
       [@key "maxDepth"]
       [@ocaml.doc
         "Maximum depth of async call stacks. Setting to `0` will effectively disable collecting async\n\
          call stacks (default)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Enables or disables async call stacks tracking."]

module Set_blackbox_execution_contexts = struct
  let name = "Debugger.setBlackboxExecutionContexts"

  type params = {
    unique_ids : string list;
       [@key "uniqueIds"] [@ocaml.doc "Array of execution context unique ids for the debugger to ignore."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Replace previous blackbox execution contexts with passed ones. Forces backend to skip\n\
   stepping/pausing in scripts in these execution contexts. VM will try to leave blackboxed script by\n\
   performing 'step in' several times, finally resorting to 'step out' if unsuccessful."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_blackbox_patterns = struct
  let name = "Debugger.setBlackboxPatterns"

  type params = {
    patterns : string list;
       [@key "patterns"] [@ocaml.doc "Array of regexps that will be used to check script url for blackbox state."]
    skip_anonymous : bool option;
       [@key "skipAnonymous"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "If true, also ignore scripts with no source url."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Replace previous blackbox patterns with passed ones. Forces backend to skip stepping/pausing in\n\
   scripts with url matching one of the patterns. VM will try to leave blackboxed script by\n\
   performing 'step in' several times, finally resorting to 'step out' if unsuccessful."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_blackboxed_ranges = struct
  let name = "Debugger.setBlackboxedRanges"

  type params = {
    script_id : Cdp_base.Runtime.Script_id.t; [@key "scriptId"] [@ocaml.doc "Id of the script."]
    positions : script_position list; [@key "positions"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Makes backend skip steps in the script in blackboxed ranges. VM will try leave blacklisted\n\
   scripts by performing 'step in' several times, finally resorting to 'step out' if unsuccessful.\n\
   Positions array contains positions where blackbox state is changed. First interval isn't\n\
   blackboxed. Array should be sorted."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_breakpoint = struct
  let name = "Debugger.setBreakpoint"

  type params = {
    location : location; [@key "location"] [@ocaml.doc "Location to set breakpoint in."]
    condition : string option;
       [@key "condition"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Expression to use as a breakpoint condition. When specified, debugger will only stop on the\n\
          breakpoint if this expression evaluates to true."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    breakpoint_id : Cdp_base.Debugger.Breakpoint_id.t;
       [@key "breakpointId"] [@ocaml.doc "Id of the created breakpoint for further reference."]
    actual_location : location; [@key "actualLocation"] [@ocaml.doc "Location this breakpoint resolved into."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Sets JavaScript breakpoint at a given location."]

module Set_instrumentation_breakpoint = struct
  let name = "Debugger.setInstrumentationBreakpoint"

  type instrumentation =
    | BeforeScriptExecution [@json.name "beforeScriptExecution"]
    | BeforeScriptWithSourceMapExecution [@json.name "beforeScriptWithSourceMapExecution"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = { instrumentation : instrumentation [@key "instrumentation"] [@ocaml.doc "Instrumentation name."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    breakpoint_id : Cdp_base.Debugger.Breakpoint_id.t;
       [@key "breakpointId"] [@ocaml.doc "Id of the created breakpoint for further reference."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Sets instrumentation breakpoint."]

module Set_breakpoint_by_url = struct
  let name = "Debugger.setBreakpointByUrl"

  type params = {
    line_number : int; [@key "lineNumber"] [@ocaml.doc "Line number to set breakpoint at."]
    url : string option;
       [@key "url"] [@option] [@json.drop_default] [@ocaml.doc "URL of the resources to set breakpoint on."]
    url_regex : string option;
       [@key "urlRegex"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Regex pattern for the URLs of the resources to set breakpoints on. Either `url` or\n\
          `urlRegex` must be specified."]
    script_hash : string option;
       [@key "scriptHash"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Script hash of the resources to set breakpoint on."]
    column_number : int option;
       [@key "columnNumber"] [@option] [@json.drop_default] [@ocaml.doc "Offset in the line to set breakpoint at."]
    condition : string option;
       [@key "condition"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Expression to use as a breakpoint condition. When specified, debugger will only stop on the\n\
          breakpoint if this expression evaluates to true."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    breakpoint_id : Cdp_base.Debugger.Breakpoint_id.t;
       [@key "breakpointId"] [@ocaml.doc "Id of the created breakpoint for further reference."]
    locations : location list;
       [@key "locations"] [@ocaml.doc "List of the locations this breakpoint resolved into upon addition."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Sets JavaScript breakpoint at given location specified either by URL or URL regex. Once this\n\
   command is issued, all existing parsed scripts will have breakpoints resolved and returned in\n\
   `locations` property. Further matching script parsing will result in subsequent\n\
   `breakpointResolved` events issued. This logical breakpoint will survive page reloads."]

module Set_breakpoint_on_function_call = struct
  let name = "Debugger.setBreakpointOnFunctionCall"

  type params = {
    object_id : Cdp_base.Runtime.Remote_object_id.t; [@key "objectId"] [@ocaml.doc "Function object id."]
    condition : string option;
       [@key "condition"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Expression to use as a breakpoint condition. When specified, debugger will\n\
          stop on the breakpoint if this expression evaluates to true."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    breakpoint_id : Cdp_base.Debugger.Breakpoint_id.t;
       [@key "breakpointId"] [@ocaml.doc "Id of the created breakpoint for further reference."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Sets JavaScript breakpoint before each call to the given function.\n\
   If another function was created from the same source as a given one,\n\
   calling it will also trigger the breakpoint."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_breakpoints_active = struct
  let name = "Debugger.setBreakpointsActive"

  type params = { active : bool [@key "active"] [@ocaml.doc "New value for breakpoints active state."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Activates / deactivates all breakpoints on the page."]

module Set_pause_on_exceptions = struct
  let name = "Debugger.setPauseOnExceptions"

  type state =
    | None_ [@json.name "none"]
    | Caught [@json.name "caught"]
    | Uncaught [@json.name "uncaught"]
    | All [@json.name "all"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = { state : state [@key "state"] [@ocaml.doc "Pause on exceptions mode."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Defines pause on exceptions state. Can be set to stop on all exceptions, uncaught exceptions,\n\
   or caught exceptions, no exceptions. Initial pause on exceptions state is `none`."]

module Set_return_value = struct
  let name = "Debugger.setReturnValue"

  type params = { new_value : Cdp_runtime_types.call_argument [@key "newValue"] [@ocaml.doc "New return value."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Changes return value in top frame. Available only at return break position."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_script_source = struct
  let name = "Debugger.setScriptSource"

  type params = {
    script_id : Cdp_base.Runtime.Script_id.t; [@key "scriptId"] [@ocaml.doc "Id of the script to edit."]
    script_source : string; [@key "scriptSource"] [@ocaml.doc "New content of the script."]
    dry_run : bool option;
       [@key "dryRun"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "If true the change will not actually be applied. Dry run may be used to get result\n\
          description without actually modifying the code."]
    allow_top_frame_editing : bool option;
       [@key "allowTopFrameEditing"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If true, then `scriptSource` is allowed to change the function on top of the stack\n\
          as long as the top-most stack frame is the only activation of that function."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type status =
    | Ok_ [@json.name "Ok"]
    | CompileError [@json.name "CompileError"]
    | BlockedByActiveGenerator [@json.name "BlockedByActiveGenerator"]
    | BlockedByActiveFunction [@json.name "BlockedByActiveFunction"]
    | BlockedByTopLevelEsModuleChange [@json.name "BlockedByTopLevelEsModuleChange"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type result = {
    call_frames : call_frame list option;
       [@key "callFrames"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "New stack trace in case editing has happened while VM was stopped."]
    stack_changed : bool option;
       [@key "stackChanged"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Whether current call stack  was modified after applying the changes."]
    async_stack_trace : Cdp_runtime_types.stack_trace option;
       [@key "asyncStackTrace"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Async stack trace, if any."]
    async_stack_trace_id : Cdp_runtime_types.stack_trace_id option;
       [@key "asyncStackTraceId"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Async stack trace, if any."]
    status : status;
       [@key "status"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Whether the operation was successful or not. Only `Ok` denotes a\n\
          successful live edit while the other enum variants denote why\n\
          the live edit failed."]
    exception_details : Cdp_runtime_types.exception_details option;
       [@key "exceptionDetails"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Exception details if any. Only present when `status` is `CompileError`."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Live edit is no longer supported and this command always fails with a \"no longer available\" error."]
[@@ocaml.deprecated "deprecated in CDP"]

module Set_skip_all_pauses = struct
  let name = "Debugger.setSkipAllPauses"

  type params = { skip : bool [@key "skip"] [@ocaml.doc "New value for skip pauses state."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Makes page not interrupt on any pauses (breakpoint, exception, dom exception etc)."]

module Set_variable_value = struct
  let name = "Debugger.setVariableValue"

  type params = {
    scope_number : int;
       [@key "scopeNumber"]
       [@ocaml.doc
         "0-based number of scope as was listed in scope chain. Only 'local', 'closure' and 'catch'\n\
          scope types are allowed. Other scopes could be manipulated manually."]
    variable_name : string; [@key "variableName"] [@ocaml.doc "Variable name."]
    new_value : Cdp_runtime_types.call_argument; [@key "newValue"] [@ocaml.doc "New variable value."]
    call_frame_id : Cdp_base.Debugger.Call_frame_id.t;
       [@key "callFrameId"] [@ocaml.doc "Id of callframe that holds variable."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Changes value of variable in a callframe. Object-based scopes are not supported and must be\nmutated manually."]

module Step_into = struct
  let name = "Debugger.stepInto"

  type params = {
    break_on_async_call : bool option;
       [@key "breakOnAsyncCall"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Debugger will pause on the execution of the first async task which was scheduled\nbefore next pause."]
    skip_list : location_range list option;
       [@key "skipList"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "The skipList specifies location ranges that should be skipped on step into."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Steps into the function call."]

module Step_out = struct
  let name = "Debugger.stepOut"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Steps out of the function call."]

module Step_over = struct
  let name = "Debugger.stepOver"

  type params = {
    skip_list : location_range list option;
       [@key "skipList"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "The skipList specifies location ranges that should be skipped on step over."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Steps over the statement."]

module Breakpoint_resolved = struct
  let name = "Debugger.breakpointResolved"

  type params = {
    breakpoint_id : Cdp_base.Debugger.Breakpoint_id.t;
       [@key "breakpointId"] [@ocaml.doc "Breakpoint unique identifier."]
    location : location; [@key "location"] [@ocaml.doc "Actual breakpoint location."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired when breakpoint is resolved to an actual script and location.\n\
   Deprecated in favor of `resolvedBreakpoints` in the `scriptParsed` event."]
[@@ocaml.deprecated "deprecated in CDP"]

module Paused = struct
  let name = "Debugger.paused"

  type reason =
    | Ambiguous [@json.name "ambiguous"]
    | Assert [@json.name "assert"]
    | CSPViolation [@json.name "CSPViolation"]
    | DebugCommand [@json.name "debugCommand"]
    | DOM [@json.name "DOM"]
    | EventListener [@json.name "EventListener"]
    | Exception [@json.name "exception"]
    | Instrumentation [@json.name "instrumentation"]
    | OOM [@json.name "OOM"]
    | Other_ [@json.name "other"]
    | PromiseRejection [@json.name "promiseRejection"]
    | XHR [@json.name "XHR"]
    | Step [@json.name "step"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    call_frames : call_frame list; [@key "callFrames"] [@ocaml.doc "Call stack the virtual machine stopped on."]
    reason : reason; [@key "reason"] [@ocaml.doc "Pause reason."]
    data : Cdp_json.t option;
       [@key "data"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Object containing break-specific auxiliary properties."]
    hit_breakpoints : string list option;
       [@key "hitBreakpoints"] [@option] [@json.drop_default] [@ocaml.doc "Hit breakpoints IDs"]
    async_stack_trace : Cdp_runtime_types.stack_trace option;
       [@key "asyncStackTrace"] [@option] [@json.drop_default] [@ocaml.doc "Async stack trace, if any."]
    async_stack_trace_id : Cdp_runtime_types.stack_trace_id option;
       [@key "asyncStackTraceId"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Async stack trace, if any."]
    async_call_stack_trace_id : Cdp_runtime_types.stack_trace_id option;
       [@key "asyncCallStackTraceId"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Never present, will be removed."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when the virtual machine stopped on breakpoint or exception or any other stop criteria."]

module Resumed = struct
  let name = "Debugger.resumed"

  type params = unit [@@deriving show, eq]

  let params_of_json (_ignored_payload : Cdp_json.t) : params = ()

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when the virtual machine resumed execution."]

module Script_failed_to_parse = struct
  let name = "Debugger.scriptFailedToParse"

  type params = {
    script_id : Cdp_base.Runtime.Script_id.t; [@key "scriptId"] [@ocaml.doc "Identifier of the script parsed."]
    url : string; [@key "url"] [@ocaml.doc "URL or name of the script parsed (if any)."]
    start_line : int;
       [@key "startLine"] [@ocaml.doc "Line offset of the script within the resource with given URL (for script tags)."]
    start_column : int;
       [@key "startColumn"] [@ocaml.doc "Column offset of the script within the resource with given URL."]
    end_line : int; [@key "endLine"] [@ocaml.doc "Last line of the script."]
    end_column : int; [@key "endColumn"] [@ocaml.doc "Length of the last line of the script."]
    execution_context_id : Cdp_base.Runtime.Execution_context_id.t;
       [@key "executionContextId"] [@ocaml.doc "Specifies script creation context."]
    hash : string; [@key "hash"] [@ocaml.doc "Content hash of the script, SHA-256."]
    build_id : string;
       [@key "buildId"]
       [@ocaml.doc
         "For Wasm modules, the content of the `build_id` custom section. For JavaScript the `debugId` magic comment."]
    execution_context_aux_data : Cdp_json.t option;
       [@key "executionContextAuxData"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Embedder-specific auxiliary data likely matching \\{isDefault: boolean, type: 'default'|'isolated'|'worker', \
          frameId: string\\}"]
    source_map_url : string option;
       [@key "sourceMapURL"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "URL of source map associated with script (if any)."]
    has_source_url : bool option;
       [@key "hasSourceURL"] [@option] [@json.drop_default] [@ocaml.doc "True, if this script has sourceURL."]
    is_module : bool option;
       [@key "isModule"] [@option] [@json.drop_default] [@ocaml.doc "True, if this script is ES6 module."]
    length : int option; [@key "length"] [@option] [@json.drop_default] [@ocaml.doc "This script length."]
    stack_trace : Cdp_runtime_types.stack_trace option;
       [@key "stackTrace"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "JavaScript top stack frame of where the script parsed event was triggered if available."]
    code_offset : int option;
       [@key "codeOffset"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "If the scriptLanguage is WebAssembly, the code section offset in the module."]
    script_language : script_language option;
       [@key "scriptLanguage"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "The language of the script."]
    embedder_name : string option;
       [@key "embedderName"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "The name the embedder supplied for this script."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when virtual machine fails to parse the script."]

module Script_parsed = struct
  let name = "Debugger.scriptParsed"

  type params = {
    script_id : Cdp_base.Runtime.Script_id.t; [@key "scriptId"] [@ocaml.doc "Identifier of the script parsed."]
    url : string; [@key "url"] [@ocaml.doc "URL or name of the script parsed (if any)."]
    start_line : int;
       [@key "startLine"] [@ocaml.doc "Line offset of the script within the resource with given URL (for script tags)."]
    start_column : int;
       [@key "startColumn"] [@ocaml.doc "Column offset of the script within the resource with given URL."]
    end_line : int; [@key "endLine"] [@ocaml.doc "Last line of the script."]
    end_column : int; [@key "endColumn"] [@ocaml.doc "Length of the last line of the script."]
    execution_context_id : Cdp_base.Runtime.Execution_context_id.t;
       [@key "executionContextId"] [@ocaml.doc "Specifies script creation context."]
    hash : string; [@key "hash"] [@ocaml.doc "Content hash of the script, SHA-256."]
    build_id : string;
       [@key "buildId"]
       [@ocaml.doc
         "For Wasm modules, the content of the `build_id` custom section. For JavaScript the `debugId` magic comment."]
    execution_context_aux_data : Cdp_json.t option;
       [@key "executionContextAuxData"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Embedder-specific auxiliary data likely matching \\{isDefault: boolean, type: 'default'|'isolated'|'worker', \
          frameId: string\\}"]
    is_live_edit : bool option;
       [@key "isLiveEdit"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "True, if this script is generated as a result of the live edit operation."]
    source_map_url : string option;
       [@key "sourceMapURL"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "URL of source map associated with script (if any)."]
    has_source_url : bool option;
       [@key "hasSourceURL"] [@option] [@json.drop_default] [@ocaml.doc "True, if this script has sourceURL."]
    is_module : bool option;
       [@key "isModule"] [@option] [@json.drop_default] [@ocaml.doc "True, if this script is ES6 module."]
    length : int option; [@key "length"] [@option] [@json.drop_default] [@ocaml.doc "This script length."]
    stack_trace : Cdp_runtime_types.stack_trace option;
       [@key "stackTrace"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "JavaScript top stack frame of where the script parsed event was triggered if available."]
    code_offset : int option;
       [@key "codeOffset"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "If the scriptLanguage is WebAssembly, the code section offset in the module."]
    script_language : script_language option;
       [@key "scriptLanguage"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "The language of the script."]
    debug_symbols : debug_symbols list option;
       [@key "debugSymbols"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "If the scriptLanguage is WebAssembly, the source of debug symbols for the module."]
    embedder_name : string option;
       [@key "embedderName"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "The name the embedder supplied for this script."]
    resolved_breakpoints : resolved_breakpoint list option;
       [@key "resolvedBreakpoints"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "The list of set breakpoints in this script if calls to `setBreakpointByUrl`\n\
          matches this script's URL or hash. Clients that use this list can ignore the\n\
          `breakpointResolved` event. They are equivalent."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired when virtual machine parses script. This event is also fired for all known and uncollected\n\
   scripts upon enabling debugger."]
