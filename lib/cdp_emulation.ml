(* Generated by cdp-gen from Chrome DevTools Protocol r1701330. DO NOT EDIT. *)

[@@@ocaml.doc "This domain emulates different environments for the page."]

include Cdp_emulation_types
open Jsonkit.Primitives

module Can_emulate = struct
  let name = "Emulation.canEmulate"

  type result = { result : bool [@key "result"] [@ocaml.doc "True if emulation is supported."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Tells whether emulation is supported."] [@@ocaml.deprecated "deprecated in CDP"]

module Clear_device_metrics_override = struct
  let name = "Emulation.clearDeviceMetricsOverride"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Clears the overridden device metrics."]

module Clear_geolocation_override = struct
  let name = "Emulation.clearGeolocationOverride"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Clears the overridden Geolocation Position and Error."]

module Reset_page_scale_factor = struct
  let name = "Emulation.resetPageScaleFactor"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Requests that page scale factor is reset to initial values."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_focus_emulation_enabled = struct
  let name = "Emulation.setFocusEmulationEnabled"

  type params = { enabled : bool [@key "enabled"] [@ocaml.doc "Whether to enable to disable focus emulation."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Enables or disables simulating a focused and active page."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_auto_dark_mode_override = struct
  let name = "Emulation.setAutoDarkModeOverride"

  type params = {
    enabled : bool option;
       [@key "enabled"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Whether to enable or disable automatic dark mode.\nIf not specified, any existing override will be cleared."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Automatically render all web contents using a dark theme."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_cpu_throttling_rate = struct
  let name = "Emulation.setCPUThrottlingRate"

  type params = {
    rate : Cdp_json.number;
       [@key "rate"] [@ocaml.doc "Throttling rate as a slowdown factor (1 is no throttle, 2 is 2x slowdown, etc)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Enables CPU throttling to emulate slow CPUs."]

module Set_default_background_color_override = struct
  let name = "Emulation.setDefaultBackgroundColorOverride"

  type params = {
    color : Cdp_dom_types.rgba option;
       [@key "color"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "RGBA of the default background color. If not specified, any existing override will be\ncleared."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Sets or clears an override of the default background color of the frame. This override is used\n\
   if the content does not specify one."]

module Set_safe_area_insets_override = struct
  let name = "Emulation.setSafeAreaInsetsOverride"

  type params = { insets : safe_area_insets [@key "insets"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Overrides the values for env(safe-area-inset-*) and env(safe-area-max-inset-*). Unset values will cause the\n\
   respective variables to be undefined, even if previously overridden."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_virtual_keyboard_geometry_override = struct
  let name = "Emulation.setVirtualKeyboardGeometryOverride"

  type params = { keyboard_rect : Cdp_dom_types.rect option [@key "keyboardRect"] [@option] [@json.drop_default] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Overrides virtual keyboard geometry in CSS pixels, relative to the top-level viewport. The\n\
   provided rect is used for navigator.virtualKeyboard.boundingRect, geometrychange events, and\n\
   env(keyboard-inset-*) values on the inspected frame. The override applies independently of\n\
   navigator.virtualKeyboard.overlaysContent so clients can preview overlay geometry without\n\
   mutating page state. Values are rounded to the nearest CSS pixel. Omitting the rect clears the\n\
   override."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_device_metrics_override = struct
  let name = "Emulation.setDeviceMetricsOverride"

  type scrollbar_type =
    | Overlay [@json.name "overlay"]
    | Default [@json.name "default"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants]

  and viewport_meta =
    | Enable [@json.name "enable"]
    | Default [@json.name "default"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    width : int;
       [@key "width"]
       [@ocaml.doc "Overriding width value in pixels (minimum 0, maximum 10000000). 0 disables the override."]
    height : int;
       [@key "height"]
       [@ocaml.doc "Overriding height value in pixels (minimum 0, maximum 10000000). 0 disables the override."]
    device_scale_factor : Cdp_json.number;
       [@key "deviceScaleFactor"] [@ocaml.doc "Overriding device scale factor value. 0 disables the override."]
    mobile : bool;
       [@key "mobile"]
       [@ocaml.doc
         "Whether to emulate mobile device. This includes viewport meta tag, overlay scrollbars, text\n\
          autosizing and more."]
    scale : Cdp_json.number option;
       [@key "scale"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Scale to apply to resulting view image."]
    screen_width : int option;
       [@key "screenWidth"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Overriding screen width value in pixels (minimum 0, maximum 10000000)."]
    screen_height : int option;
       [@key "screenHeight"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Overriding screen height value in pixels (minimum 0, maximum 10000000)."]
    position_x : int option;
       [@key "positionX"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Overriding view X position on screen in pixels (minimum 0, maximum 10000000)."]
    position_y : int option;
       [@key "positionY"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Overriding view Y position on screen in pixels (minimum 0, maximum 10000000)."]
    dont_set_visible_size : bool option;
       [@key "dontSetVisibleSize"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Do not set visible view size, rely upon explicit setVisibleSize call."]
    screen_orientation : screen_orientation option;
       [@key "screenOrientation"] [@option] [@json.drop_default] [@ocaml.doc "Screen orientation override."]
    viewport : Cdp_page_types.viewport option;
       [@key "viewport"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If set, the visible area of the page will be overridden to this viewport. This viewport\n\
          change is not observed by the page, e.g. viewport-relative elements do not change positions."]
    display_feature : display_feature option;
       [@key "displayFeature"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If set, the display feature of a multi-segment screen. If not set, multi-segment support\n\
          is turned-off.\n\
          Deprecated, use Emulation.setDisplayFeaturesOverride."]
    device_posture : device_posture option;
       [@key "devicePosture"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If set, the posture of a foldable device. If not set the posture is set\n\
          to continuous.\n\
          Deprecated, use Emulation.setDevicePostureOverride."]
    scrollbar_type : scrollbar_type option;
       [@key "scrollbarType"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Scrollbar type. Default: `default`."]
    screen_orientation_lock_emulation : bool option;
       [@key "screenOrientationLockEmulation"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If set to true, enables screen orientation lock emulation, which\n\
          intercepts screen.orientation.lock() calls from the page and reports\n\
          orientation changes via screenOrientationLockChanged events. This is\n\
          useful for emulating mobile device orientation lock behavior in\n\
          responsive design mode."]
    viewport_meta : viewport_meta option;
       [@key "viewportMeta"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "Viewport meta tag behavior. Default: `default`. Note: if `mobile` is `true`,\n\
          the viewport meta tag is always enabled."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Overrides the values of device screen dimensions (window.screen.width, window.screen.height,\n\
   window.innerWidth, window.innerHeight, and \"device-width\"/\"device-height\"-related CSS media\n\
   query results)."]

module Set_device_posture_override = struct
  let name = "Emulation.setDevicePostureOverride"

  type params = { posture : device_posture [@key "posture"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Start reporting the given posture value to the Device Posture API.\n\
   This override can also be set in setDeviceMetricsOverride()."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Clear_device_posture_override = struct
  let name = "Emulation.clearDevicePostureOverride"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc
  "Clears a device posture override set with either setDeviceMetricsOverride()\n\
   or setDevicePostureOverride() and starts using posture information from the\n\
   platform again.\n\
   Does nothing if no override is set."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_display_features_override = struct
  let name = "Emulation.setDisplayFeaturesOverride"

  type params = { features : display_feature list [@key "features"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Start using the given display features to pupulate the Viewport Segments API.\n\
   This override can also be set in setDeviceMetricsOverride()."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Clear_display_features_override = struct
  let name = "Emulation.clearDisplayFeaturesOverride"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc
  "Clears the display features override set with either setDeviceMetricsOverride()\n\
   or setDisplayFeaturesOverride() and starts using display features from the\n\
   platform again.\n\
   Does nothing if no override is set."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_scrollbars_hidden = struct
  let name = "Emulation.setScrollbarsHidden"

  type params = { hidden : bool [@key "hidden"] [@ocaml.doc "Whether scrollbars should be always hidden."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_document_cookie_disabled = struct
  let name = "Emulation.setDocumentCookieDisabled"

  type params = { disabled : bool [@key "disabled"] [@ocaml.doc "Whether document.coookie API should be disabled."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_emit_touch_events_for_mouse = struct
  let name = "Emulation.setEmitTouchEventsForMouse"

  type configuration =
    | Mobile [@json.name "mobile"]
    | Desktop [@json.name "desktop"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    enabled : bool; [@key "enabled"] [@ocaml.doc "Whether touch emulation based on mouse input should be enabled."]
    configuration : configuration option;
       [@key "configuration"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Touch/gesture events configuration. Default: current platform."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_emulated_media = struct
  let name = "Emulation.setEmulatedMedia"

  type params = {
    media : string option;
       [@key "media"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Media type to emulate. Empty string disables the override."]
    features : media_feature list option;
       [@key "features"] [@option] [@json.drop_default] [@ocaml.doc "Media features to emulate."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Emulates the given media type or media feature for CSS media queries."]

module Set_emulated_vision_deficiency = struct
  let name = "Emulation.setEmulatedVisionDeficiency"

  type type_ =
    | None_ [@json.name "none"]
    | BlurredVision [@json.name "blurredVision"]
    | ReducedContrast [@json.name "reducedContrast"]
    | Achromatopsia [@json.name "achromatopsia"]
    | Deuteranopia [@json.name "deuteranopia"]
    | Protanopia [@json.name "protanopia"]
    | Tritanopia [@json.name "tritanopia"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    type_ : type_;
       [@key "type"]
       [@ocaml.doc
         "Vision deficiency to emulate. Order: best-effort emulations come first, followed by any\n\
          physiologically accurate emulations for medically recognized color vision deficiencies."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Emulates the given vision deficiency."]

module Set_emulated_os_text_scale = struct
  let name = "Emulation.setEmulatedOSTextScale"

  type params = { scale : Cdp_json.number option [@key "scale"] [@option] [@json.drop_default] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Emulates the given OS text scale."]

module Set_geolocation_override = struct
  let name = "Emulation.setGeolocationOverride"

  type params = {
    latitude : Cdp_json.number option; [@key "latitude"] [@option] [@json.drop_default] [@ocaml.doc "Mock latitude"]
    longitude : Cdp_json.number option; [@key "longitude"] [@option] [@json.drop_default] [@ocaml.doc "Mock longitude"]
    accuracy : Cdp_json.number option; [@key "accuracy"] [@option] [@json.drop_default] [@ocaml.doc "Mock accuracy"]
    altitude : Cdp_json.number option; [@key "altitude"] [@option] [@json.drop_default] [@ocaml.doc "Mock altitude"]
    altitude_accuracy : Cdp_json.number option;
       [@key "altitudeAccuracy"] [@option] [@json.drop_default] [@ocaml.doc "Mock altitudeAccuracy"]
    heading : Cdp_json.number option; [@key "heading"] [@option] [@json.drop_default] [@ocaml.doc "Mock heading"]
    speed : Cdp_json.number option; [@key "speed"] [@option] [@json.drop_default] [@ocaml.doc "Mock speed"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Overrides the Geolocation Position or Error. Omitting latitude, longitude or\n\
   accuracy emulates position unavailable."]

module Get_overridden_sensor_information = struct
  let name = "Emulation.getOverriddenSensorInformation"

  type params = { type_ : sensor_type [@key "type"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { requested_sampling_frequency : Cdp_json.number [@key "requestedSamplingFrequency"] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_sensor_override_enabled = struct
  let name = "Emulation.setSensorOverrideEnabled"

  type params = {
    enabled : bool; [@key "enabled"]
    type_ : sensor_type; [@key "type"]
    metadata : sensor_metadata option; [@key "metadata"] [@option] [@json.drop_default]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Overrides a platform sensor of a given type. If |enabled| is true, calls to\n\
   Sensor.start() will use a virtual sensor as backend rather than fetching\n\
   data from a real hardware sensor. Otherwise, existing virtual\n\
   sensor-backend Sensor objects will fire an error event and new calls to\n\
   Sensor.start() will attempt to use a real sensor instead."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_sensor_override_readings = struct
  let name = "Emulation.setSensorOverrideReadings"

  type params = {
    type_ : sensor_type; [@key "type"]
    reading : sensor_reading; [@key "reading"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Updates the sensor readings reported by a sensor type previously overridden\nby setSensorOverrideEnabled."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_pressure_source_override_enabled = struct
  let name = "Emulation.setPressureSourceOverrideEnabled"

  type params = {
    enabled : bool; [@key "enabled"]
    source : pressure_source; [@key "source"]
    metadata : pressure_metadata option; [@key "metadata"] [@option] [@json.drop_default]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Overrides a pressure source of a given type, as used by the Compute\n\
   Pressure API, so that updates to PressureObserver.observe() are provided\n\
   via setPressureStateOverride instead of being retrieved from\n\
   platform-provided telemetry data."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_pressure_state_override = struct
  let name = "Emulation.setPressureStateOverride"

  type params = {
    source : pressure_source; [@key "source"]
    state : pressure_state; [@key "state"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Provides a given pressure state that will be processed and eventually be\n\
   delivered to PressureObserver users. |source| must have been previously\n\
   overridden by setPressureSourceOverrideEnabled."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_idle_override = struct
  let name = "Emulation.setIdleOverride"

  type params = {
    is_user_active : bool; [@key "isUserActive"] [@ocaml.doc "Mock isUserActive"]
    is_screen_unlocked : bool; [@key "isScreenUnlocked"] [@ocaml.doc "Mock isScreenUnlocked"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Overrides the Idle state."]

module Clear_idle_override = struct
  let name = "Emulation.clearIdleOverride"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Clears Idle state overrides."]

module Set_navigator_overrides = struct
  let name = "Emulation.setNavigatorOverrides"

  type params = { platform : string [@key "platform"] [@ocaml.doc "The platform navigator.platform should return."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Overrides value returned by the javascript navigator object."]
[@@ocaml.deprecated "deprecated in CDP"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_page_scale_factor = struct
  let name = "Emulation.setPageScaleFactor"

  type params = { page_scale_factor : Cdp_json.number [@key "pageScaleFactor"] [@ocaml.doc "Page scale factor."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Sets a specified page scale factor."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_script_execution_disabled = struct
  let name = "Emulation.setScriptExecutionDisabled"

  type params = { value : bool [@key "value"] [@ocaml.doc "Whether script execution should be disabled in the page."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Switches script execution in the page."]

module Set_touch_emulation_enabled = struct
  let name = "Emulation.setTouchEmulationEnabled"

  type params = {
    enabled : bool; [@key "enabled"] [@ocaml.doc "Whether the touch event emulation should be enabled."]
    max_touch_points : int option;
       [@key "maxTouchPoints"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Maximum touch points supported. Defaults to one."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Enables touch on platforms which do not support them."]

module Set_virtual_time_policy = struct
  let name = "Emulation.setVirtualTimePolicy"

  type params = {
    policy : virtual_time_policy; [@key "policy"]
    budget : Cdp_json.number option;
       [@key "budget"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "If set, after this many virtual milliseconds have elapsed virtual time will be paused and a\n\
          virtualTimeBudgetExpired event is sent."]
    max_virtual_time_task_starvation_count : int option;
       [@key "maxVirtualTimeTaskStarvationCount"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "If set this specifies the maximum number of tasks that can be run before virtual is forced\n\
          forwards to prevent deadlock."]
    initial_virtual_time : Cdp_base.Network.Time_since_epoch.t option;
       [@key "initialVirtualTime"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "If set, base::Time::Now will be overridden to initially return this value."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    virtual_time_ticks_base : Cdp_json.number;
       [@key "virtualTimeTicksBase"]
       [@ocaml.doc "Absolute timestamp at which virtual time was first enabled (up time in milliseconds)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Turns on virtual time for all frames (replacing real-time with a synthetic time source) and sets\n\
   the current virtual time policy.  Note this supersedes any previous time budget."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_locale_override = struct
  let name = "Emulation.setLocaleOverride"

  type params = {
    locale : string option;
       [@key "locale"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "ICU style C locale (e.g. \"en_US\"). If not specified or empty, disables the override and\n\
          restores default host system locale."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Overrides default host system locale with the specified one."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_timezone_override = struct
  let name = "Emulation.setTimezoneOverride"

  type params = {
    timezone_id : string;
       [@key "timezoneId"]
       [@ocaml.doc
         "The timezone identifier. List of supported timezones:\n\
          https://source.chromium.org/chromium/chromium/deps/icu.git/+/faee8bc70570192d82d2978a71e2a615788597d1:source/data/misc/metaZones.txt\n\
          If empty, disables the override and restores default host system timezone."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Overrides default host system timezone with the specified one."]

module Set_visible_size = struct
  let name = "Emulation.setVisibleSize"

  type params = {
    width : int; [@key "width"] [@ocaml.doc "Frame width (DIP)."]
    height : int; [@key "height"] [@ocaml.doc "Frame height (DIP)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Resizes the frame/viewport of the page. Note that this does not affect the frame's container\n\
   (e.g. browser window). Can be used to produce screenshots of the specified size. Not supported\n\
   on Android."]
[@@ocaml.deprecated "deprecated in CDP"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_disabled_image_types = struct
  let name = "Emulation.setDisabledImageTypes"

  type params = { image_types : disabled_image_type list [@key "imageTypes"] [@ocaml.doc "Image types to disable."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_data_saver_override = struct
  let name = "Emulation.setDataSaverOverride"

  type params = {
    data_saver_enabled : bool option;
       [@key "dataSaverEnabled"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Override value. Omitting the parameter disables the override."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Override the value of navigator.connection.saveData"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_hardware_concurrency_override = struct
  let name = "Emulation.setHardwareConcurrencyOverride"

  type params = {
    hardware_concurrency : int; [@key "hardwareConcurrency"] [@ocaml.doc "Hardware concurrency to report"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_cpu_performance_override = struct
  let name = "Emulation.setCPUPerformanceOverride"

  type performance_tier =
    | Unknown [@json.name "unknown"]
    | Low [@json.name "low"]
    | Mid [@json.name "mid"]
    | High [@json.name "high"]
    | Ultra [@json.name "ultra"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    performance_tier : performance_tier option;
       [@key "performanceTier"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Override value. Omitting the parameter disables the override."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Overrides the value of navigator.cpuPerformance"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_user_agent_override = struct
  let name = "Emulation.setUserAgentOverride"

  type params = {
    user_agent : string; [@key "userAgent"] [@ocaml.doc "User agent to use."]
    accept_language : string option;
       [@key "acceptLanguage"] [@option] [@json.drop_default] [@ocaml.doc "Browser language to emulate."]
    platform : string option;
       [@key "platform"] [@option] [@json.drop_default] [@ocaml.doc "The platform navigator.platform should return."]
    user_agent_metadata : user_agent_metadata option;
       [@key "userAgentMetadata"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "To be sent in Sec-CH-UA-* headers and returned in navigator.userAgentData"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Allows overriding user agent with the given string.\n\
   `userAgentMetadata` must be set for Client Hint headers to be sent."]

module Set_automation_override = struct
  let name = "Emulation.setAutomationOverride"

  type params = { enabled : bool [@key "enabled"] [@ocaml.doc "Whether the override should be enabled."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Allows overriding the automation flag."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_small_viewport_height_difference_override = struct
  let name = "Emulation.setSmallViewportHeightDifferenceOverride"

  type params = {
    difference : int;
       [@key "difference"]
       [@ocaml.doc
         "This will cause an element of size 100svh to be `difference` pixels smaller than an element\nof size 100lvh."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Allows overriding the difference between the small and large viewport sizes, which determine the\n\
   value of the `svh` and `lvh` unit, respectively. Only supported for top-level frames."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_screen_infos = struct
  let name = "Emulation.getScreenInfos"

  type result = { screen_infos : screen_info list [@key "screenInfos"] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc
  "Returns device's screen configuration. In headful mode, the physical screens configuration is returned,\n\
   whereas in headless mode, a virtual headless screen configuration is provided instead."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Add_screen = struct
  let name = "Emulation.addScreen"

  type params = {
    left : int; [@key "left"] [@ocaml.doc "Offset of the left edge of the screen in pixels."]
    top : int; [@key "top"] [@ocaml.doc "Offset of the top edge of the screen in pixels."]
    width : int; [@key "width"] [@ocaml.doc "The width of the screen in pixels."]
    height : int; [@key "height"] [@ocaml.doc "The height of the screen in pixels."]
    work_area_insets : work_area_insets option;
       [@key "workAreaInsets"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Specifies the screen's work area. Default is entire screen."]
    device_pixel_ratio : Cdp_json.number option;
       [@key "devicePixelRatio"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Specifies the screen's device pixel ratio. Default is 1."]
    rotation : int option;
       [@key "rotation"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Specifies the screen's rotation angle. Available values are 0, 90, 180 and 270. Default is 0."]
    color_depth : int option;
       [@key "colorDepth"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Specifies the screen's color depth in bits. Default is 24."]
    label : string option;
       [@key "label"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Specifies the descriptive label for the screen. Default is none."]
    is_internal : bool option;
       [@key "isInternal"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Indicates whether the screen is internal to the device or external, attached to the device. Default is false."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { screen_info : screen_info [@key "screenInfo"] } [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Add a new screen to the device. Only supported in headless mode."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Update_screen = struct
  let name = "Emulation.updateScreen"

  type params = {
    screen_id : Cdp_base.Emulation.Screen_id.t; [@key "screenId"] [@ocaml.doc "Target screen identifier."]
    left : int option;
       [@key "left"] [@option] [@json.drop_default] [@ocaml.doc "Offset of the left edge of the screen in pixels."]
    top : int option;
       [@key "top"] [@option] [@json.drop_default] [@ocaml.doc "Offset of the top edge of the screen in pixels."]
    width : int option; [@key "width"] [@option] [@json.drop_default] [@ocaml.doc "The width of the screen in pixels."]
    height : int option;
       [@key "height"] [@option] [@json.drop_default] [@ocaml.doc "The height of the screen in pixels."]
    work_area_insets : work_area_insets option;
       [@key "workAreaInsets"] [@option] [@json.drop_default] [@ocaml.doc "Specifies the screen's work area."]
    device_pixel_ratio : Cdp_json.number option;
       [@key "devicePixelRatio"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Specifies the screen's device pixel ratio."]
    rotation : int option;
       [@key "rotation"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Specifies the screen's rotation angle. Available values are 0, 90, 180 and 270."]
    color_depth : int option;
       [@key "colorDepth"] [@option] [@json.drop_default] [@ocaml.doc "Specifies the screen's color depth in bits."]
    label : string option;
       [@key "label"] [@option] [@json.drop_default] [@ocaml.doc "Specifies the descriptive label for the screen."]
    is_internal : bool option;
       [@key "isInternal"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Indicates whether the screen is internal to the device or external, attached to the device. Default is false."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { screen_info : screen_info [@key "screenInfo"] } [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Updates specified screen parameters. Only supported in headless mode."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Remove_screen = struct
  let name = "Emulation.removeScreen"

  type params = { screen_id : Cdp_base.Emulation.Screen_id.t [@key "screenId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Remove screen from the device. Only supported in headless mode."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_primary_screen = struct
  let name = "Emulation.setPrimaryScreen"

  type params = { screen_id : Cdp_base.Emulation.Screen_id.t [@key "screenId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Set primary screen. Only supported in headless mode.\n\
   Note that this changes the coordinate system origin to the top-left\n\
   of the new primary screen, updating the bounds and work areas\n\
   of all existing screens accordingly."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Virtual_time_budget_expired = struct
  let name = "Emulation.virtualTimeBudgetExpired"

  type params = unit [@@deriving show, eq]

  let params_of_json (_ignored_payload : Cdp_json.t) : params = ()

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Notification sent after the virtual time budget for the current VirtualTimePolicy has run out."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Screen_orientation_lock_changed = struct
  let name = "Emulation.screenOrientationLockChanged"

  type params = {
    locked : bool; [@key "locked"] [@ocaml.doc "Whether the screen orientation is currently locked."]
    orientation : screen_orientation option;
       [@key "orientation"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The orientation lock type requested by the page. Only set when locked is true."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired when a page calls screen.orientation.lock() or screen.orientation.unlock()\n\
   while device emulation is enabled. This allows the DevTools frontend to update the\n\
   emulated device orientation accordingly."]
[@@alert experimental "experimental in CDP, may change with Chrome"]
