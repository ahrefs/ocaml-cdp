(* Generated by cdp-gen from Chrome DevTools Protocol r1698617. DO NOT EDIT. *)

[@@@ocaml.doc "Input/Output operations for streams produced by DevTools."]

include Cdp_io_types
open Jsonkit.Primitives

module Close = struct
  let name = "IO.close"

  type params = { handle : Cdp_base.Io.Stream_handle.t [@key "handle"] [@ocaml.doc "Handle of the stream to close."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Close the stream, discard any temporary backing storage."]

module Read = struct
  let name = "IO.read"

  type params = {
    handle : Cdp_base.Io.Stream_handle.t; [@key "handle"] [@ocaml.doc "Handle of the stream to read."]
    offset : int option;
       [@key "offset"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Seek to the specified offset before reading (if not specified, proceed with offset\n\
          following the last read). Some types of streams may only support sequential reads."]
    size : int option;
       [@key "size"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Maximum number of bytes to read (left upon the agent discretion if not specified)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    base64_encoded : bool option;
       [@key "base64Encoded"] [@option] [@json.drop_default] [@ocaml.doc "Set if the data is base64-encoded"]
    data : string; [@key "data"] [@ocaml.doc "Data that were read."]
    eof : bool; [@key "eof"] [@ocaml.doc "Set if the end-of-file condition occurred while reading."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Read a chunk of the stream"]

module Resolve_blob = struct
  let name = "IO.resolveBlob"

  type params = {
    object_id : Cdp_base.Runtime.Remote_object_id.t; [@key "objectId"] [@ocaml.doc "Object id of a Blob object wrapper."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { uuid : string [@key "uuid"] [@ocaml.doc "UUID of the specified Blob."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Return UUID of Blob object specified by a remote object id."]
