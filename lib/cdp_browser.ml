(* Generated by cdp-gen from Chrome DevTools Protocol r1701330. DO NOT EDIT. *)

[@@@ocaml.doc "The Browser domain defines methods and events for browser managing."]

include Cdp_browser_types
open Jsonkit.Primitives

module Set_permission = struct
  let name = "Browser.setPermission"

  type params = {
    permission : permission_descriptor; [@key "permission"] [@ocaml.doc "Descriptor of permission to override."]
    setting : permission_setting; [@key "setting"] [@ocaml.doc "Setting of the permission."]
    origin : string option;
       [@key "origin"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Embedding origin the permission applies to, all origins if not specified."]
    embedded_origin : string option;
       [@key "embeddedOrigin"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Embedded origin the permission applies to. It is ignored unless the embedding origin is\n\
          present and valid. If the embedding origin is provided but the embedded origin isn't, the\n\
          embedding origin is used as the embedded origin."]
    browser_context_id : Cdp_base.Browser.Browser_context_id.t option;
       [@key "browserContextId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Context to override. When omitted, default browser context is used."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Set permission settings for given embedding and embedded origins."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Grant_permissions = struct
  let name = "Browser.grantPermissions"

  type params = {
    permissions : permission_type list; [@key "permissions"]
    origin : string option;
       [@key "origin"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Origin the permission applies to, all origins if not specified."]
    browser_context_id : Cdp_base.Browser.Browser_context_id.t option;
       [@key "browserContextId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "BrowserContext to override permissions. When omitted, default browser context is used."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Grant specific permissions to the given origin and reject all others. Deprecated. Use\nsetPermission instead."]
[@@ocaml.deprecated "deprecated in CDP"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Reset_permissions = struct
  let name = "Browser.resetPermissions"

  type params = {
    browser_context_id : Cdp_base.Browser.Browser_context_id.t option;
       [@key "browserContextId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "BrowserContext to reset permissions. When omitted, default browser context is used."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Reset all permission management for all origins."]

module Set_download_behavior = struct
  let name = "Browser.setDownloadBehavior"

  type behavior =
    | Deny [@json.name "deny"]
    | Allow [@json.name "allow"]
    | AllowAndName [@json.name "allowAndName"]
    | Default [@json.name "default"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    behavior : behavior;
       [@key "behavior"]
       [@ocaml.doc
         "Whether to allow all or deny all download requests, or use default Chrome behavior if\n\
          available (otherwise deny). |allowAndName| allows download and names files according to\n\
          their download guids."]
    browser_context_id : Cdp_base.Browser.Browser_context_id.t option;
       [@key "browserContextId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "BrowserContext to set download behavior. When omitted, default browser context is used."]
    download_path : string option;
       [@key "downloadPath"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "The default path to save downloaded files to. This is required if behavior is set to 'allow'\n\
          or 'allowAndName'."]
    events_enabled : bool option;
       [@key "eventsEnabled"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Whether to emit download events (defaults to false)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Set the behavior when downloading a file."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Cancel_download = struct
  let name = "Browser.cancelDownload"

  type params = {
    guid : string; [@key "guid"] [@ocaml.doc "Global unique identifier of the download."]
    browser_context_id : Cdp_base.Browser.Browser_context_id.t option;
       [@key "browserContextId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "BrowserContext to perform the action in. When omitted, default browser context is used."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Cancel a download if in progress"] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Close = struct
  let name = "Browser.close"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Close browser gracefully."]

module Crash = struct
  let name = "Browser.crash"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Crashes browser on the main thread."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Crash_gpu_process = struct
  let name = "Browser.crashGpuProcess"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Crashes GPU process."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_version = struct
  let name = "Browser.getVersion"

  type result = {
    protocol_version : string; [@key "protocolVersion"] [@ocaml.doc "Protocol version."]
    product : string; [@key "product"] [@ocaml.doc "Product name."]
    revision : string; [@key "revision"] [@ocaml.doc "Product revision."]
    user_agent : string; [@key "userAgent"] [@ocaml.doc "User-Agent."]
    js_version : string; [@key "jsVersion"] [@ocaml.doc "V8 version."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Returns version information."]

module Get_browser_command_line = struct
  let name = "Browser.getBrowserCommandLine"

  type result = { arguments : string list [@key "arguments"] [@ocaml.doc "Commandline parameters"] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc
  "Returns the command line switches for the browser process if, and only if\n\
   --enable-automation is on the commandline."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Add_mock_camera = struct
  let name = "Browser.addMockCamera"

  type params = {
    device_id : string;
       [@key "deviceId"]
       [@ocaml.doc
         "Required non-empty identifier for the mock camera. This is mapped to an\n\
          internal virtual-device identifier and is not the MediaDeviceInfo.deviceId\n\
          exposed to the page."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Adds or updates a mock camera in the shared video capture device list for\n\
   test automation. The mock camera is not scoped to a particular page or\n\
   frame and is removed when the DevTools session that created it disconnects."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_histograms = struct
  let name = "Browser.getHistograms"

  type params = {
    query : string option;
       [@key "query"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Requested substring in name. Only histograms which have query as a\n\
          substring in their name are extracted. An empty or absent query returns\n\
          all histograms."]
    delta : bool option;
       [@key "delta"] [@option] [@json.drop_default] [@ocaml.doc "If true, retrieve delta since last delta call."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { histograms : histogram list [@key "histograms"] [@ocaml.doc "Histograms."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Get Chrome histograms."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_histogram = struct
  let name = "Browser.getHistogram"

  type params = {
    name : string; [@key "name"] [@ocaml.doc "Requested histogram name."]
    delta : bool option;
       [@key "delta"] [@option] [@json.drop_default] [@ocaml.doc "If true, retrieve delta since last delta call."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { histogram : histogram [@key "histogram"] [@ocaml.doc "Histogram."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Get a Chrome histogram by name."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_window_bounds = struct
  let name = "Browser.getWindowBounds"

  type params = { window_id : Cdp_base.Browser.Window_id.t [@key "windowId"] [@ocaml.doc "Browser window id."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    bounds : bounds;
       [@key "bounds"]
       [@ocaml.doc
         "Bounds information of the window. When window state is 'minimized', the restored window\n\
          position and size are returned."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Get position and size of the browser window."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_window_for_target = struct
  let name = "Browser.getWindowForTarget"

  type params = {
    target_id : Cdp_base.Target.Target_id.t option;
       [@key "targetId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Devtools agent host id. If called as a part of the session, associated targetId is used."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    window_id : Cdp_base.Browser.Window_id.t; [@key "windowId"] [@ocaml.doc "Browser window id."]
    bounds : bounds;
       [@key "bounds"]
       [@ocaml.doc
         "Bounds information of the window. When window state is 'minimized', the restored window\n\
          position and size are returned."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Get the browser window that contains the devtools target."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_window_bounds = struct
  let name = "Browser.setWindowBounds"

  type params = {
    window_id : Cdp_base.Browser.Window_id.t; [@key "windowId"] [@ocaml.doc "Browser window id."]
    bounds : bounds;
       [@key "bounds"]
       [@ocaml.doc
         "New window bounds. The 'minimized', 'maximized' and 'fullscreen' states cannot be combined\n\
          with 'left', 'top', 'width' or 'height'. Leaves unspecified fields unchanged."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Set position and/or size of the browser window."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_contents_size = struct
  let name = "Browser.setContentsSize"

  type params = {
    window_id : Cdp_base.Browser.Window_id.t; [@key "windowId"] [@ocaml.doc "Browser window id."]
    width : int option;
       [@key "width"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "The window contents width in DIP. Assumes current width if omitted.\n\
          Must be specified if 'height' is omitted."]
    height : int option;
       [@key "height"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "The window contents height in DIP. Assumes current height if omitted.\n\
          Must be specified if 'width' is omitted."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Set size of the browser contents resizing browser window as necessary."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_dock_tile = struct
  let name = "Browser.setDockTile"

  type params = {
    badge_label : string option; [@key "badgeLabel"] [@option] [@json.drop_default]
    image : string option;
       [@key "image"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Png encoded image. (Encoded as a base64 string when passed over JSON)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Set dock tile details, platform-specific."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Execute_browser_command = struct
  let name = "Browser.executeBrowserCommand"

  type params = { command_id : browser_command_id [@key "commandId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Invoke custom browser commands used by telemetry."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Add_privacy_sandbox_enrollment_override = struct
  let name = "Browser.addPrivacySandboxEnrollmentOverride"

  type params = { url : string [@key "url"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Allows a site to use privacy sandbox features that require enrollment\n\
   without the site actually being enrolled. Only supported on page targets."]

module Get_global_privacy_control = struct
  let name = "Browser.getGlobalPrivacyControl"

  type result = { gpc : bool [@key "gpc"] } [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc
  "Gets the current globally-applied privacy control status\nSee https://www.w3.org/TR/gpc/#get-global-privacy-control"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_global_privacy_control = struct
  let name = "Browser.setGlobalPrivacyControl"

  type params = { gpc : bool [@key "gpc"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { gpc : bool [@key "gpc"] } [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Sets and then gets the current globally-applied privacy control status\n\
   See https://www.w3.org/TR/gpc/#set-global-privacy-control"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Download_will_begin = struct
  let name = "Browser.downloadWillBegin"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t;
       [@key "frameId"] [@ocaml.doc "Id of the frame that caused the download to begin."]
    guid : string; [@key "guid"] [@ocaml.doc "Global unique identifier of the download."]
    url : string; [@key "url"] [@ocaml.doc "URL of the resource being downloaded."]
    suggested_filename : string;
       [@key "suggestedFilename"]
       [@ocaml.doc "Suggested file name of the resource (the actual name of the file saved on disk may differ)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when page is about to start a download."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Download_progress = struct
  let name = "Browser.downloadProgress"

  type state =
    | InProgress [@json.name "inProgress"]
    | Completed [@json.name "completed"]
    | Canceled [@json.name "canceled"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    guid : string; [@key "guid"] [@ocaml.doc "Global unique identifier of the download."]
    total_bytes : Cdp_json.number; [@key "totalBytes"] [@ocaml.doc "Total expected bytes to download."]
    received_bytes : Cdp_json.number; [@key "receivedBytes"] [@ocaml.doc "Total bytes received."]
    state : state; [@key "state"] [@ocaml.doc "Download status."]
    file_path : string option;
       [@key "filePath"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If download is \"completed\", provides the path of the downloaded file.\n\
          Depending on the platform, it is not guaranteed to be set, nor the file\n\
          is guaranteed to exist."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when download makes progress. Last call has |done| == true."]
[@@alert experimental "experimental in CDP, may change with Chrome"]
