(* Generated by cdp-gen from Chrome DevTools Protocol r1701330. DO NOT EDIT. *)

[@@@ocaml.doc "Actions and events related to the inspected page belong to the page domain."]

include Cdp_page_types
open Jsonkit.Primitives

module Add_script_to_evaluate_on_load = struct
  let name = "Page.addScriptToEvaluateOnLoad"

  type params = { script_source : string [@key "scriptSource"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    identifier : Cdp_base.Page.Script_identifier.t; [@key "identifier"] [@ocaml.doc "Identifier of the added script."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Deprecated, please use addScriptToEvaluateOnNewDocument instead."]
[@@ocaml.deprecated "deprecated in CDP"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Add_script_to_evaluate_on_new_document = struct
  let name = "Page.addScriptToEvaluateOnNewDocument"

  type params = {
    source : string; [@key "source"]
    world_name : string option;
       [@key "worldName"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If specified, creates an isolated world with the given name and evaluates given script in it.\n\
          This world name will be used as the ExecutionContextDescription::name when the corresponding\n\
          event is emitted."]
    include_command_line_api : bool option;
       [@key "includeCommandLineAPI"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Specifies whether command line API should be available to the script, defaults\nto false."]
    run_immediately : bool option;
       [@key "runImmediately"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "If true, runs the script immediately on existing execution contexts or worlds.\nDefault: false."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    identifier : Cdp_base.Page.Script_identifier.t; [@key "identifier"] [@ocaml.doc "Identifier of the added script."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Evaluates given script in every frame upon creation (before loading frame's scripts)."]

module Bring_to_front = struct
  let name = "Page.bringToFront"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Brings page to front (activates tab)."]

module Capture_screenshot = struct
  let name = "Page.captureScreenshot"

  type format =
    | Jpeg [@json.name "jpeg"]
    | Png [@json.name "png"]
    | Webp [@json.name "webp"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    format : format option;
       [@key "format"] [@option] [@json.drop_default] [@ocaml.doc "Image compression format (defaults to png)."]
    quality : int option;
       [@key "quality"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Compression quality from range \\[0..100\\] (jpeg only)."]
    clip : viewport option;
       [@key "clip"] [@option] [@json.drop_default] [@ocaml.doc "Capture the screenshot of a given region only."]
    from_surface : bool option;
       [@key "fromSurface"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Capture the screenshot from the surface, rather than the view. Defaults to true."]
    capture_beyond_viewport : bool option;
       [@key "captureBeyondViewport"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Capture the screenshot beyond the viewport. Defaults to false."]
    optimize_for_speed : bool option;
       [@key "optimizeForSpeed"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Optimize image encoding for speed, not for resulting size (defaults to false)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    data : string;
       [@key "data"] [@ocaml.doc "Base64-encoded image data. (Encoded as a base64 string when passed over JSON)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Capture page screenshot."]

module Capture_snapshot = struct
  let name = "Page.captureSnapshot"

  type format =
    | Mhtml [@json.name "mhtml"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    format : format option; [@key "format"] [@option] [@json.drop_default] [@ocaml.doc "Format (defaults to mhtml)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { data : string [@key "data"] [@ocaml.doc "Serialized page data."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Returns a snapshot of the page as a string. For MHTML format, the serialization includes\n\
   iframes, shadow DOM, external resources, and element-inline styles."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Clear_device_metrics_override = struct
  let name = "Page.clearDeviceMetricsOverride"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Clears the overridden device metrics."]
[@@ocaml.deprecated "deprecated in CDP, redirected to the Emulation domain"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Clear_device_orientation_override = struct
  let name = "Page.clearDeviceOrientationOverride"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Clears the overridden Device Orientation."]
[@@ocaml.deprecated "deprecated in CDP, redirected to the DeviceOrientation domain"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Clear_geolocation_override = struct
  let name = "Page.clearGeolocationOverride"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Clears the overridden Geolocation Position and Error."]
[@@ocaml.deprecated "deprecated in CDP, redirected to the Emulation domain"]

module Create_isolated_world = struct
  let name = "Page.createIsolatedWorld"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t;
       [@key "frameId"] [@ocaml.doc "Id of the frame in which the isolated world should be created."]
    world_name : string option;
       [@key "worldName"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "An optional name which is reported in the Execution Context."]
    grant_univeral_access : bool option;
       [@key "grantUniveralAccess"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Whether or not universal access should be granted to the isolated world. This is a powerful\n\
          option, use with caution."]
    content_security_policy : string option;
       [@key "contentSecurityPolicy"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "An optional content security policy to set for the isolated world.\n\
          If omitted, any existing CSP for the world will be cleared.\n\
          Note that clearing or updating the CSP does not immediately affect the active\n\
          context in the same document because LocalDOMWindow caches the\n\
          ContentSecurityPolicy object. The change takes effect on subsequent\n\
          navigations when a new window context is created."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    execution_context_id : Cdp_base.Runtime.Execution_context_id.t;
       [@key "executionContextId"] [@ocaml.doc "Execution context of the isolated world."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Creates an isolated world for the given frame."]

module Delete_cookie = struct
  let name = "Page.deleteCookie"

  type params = {
    cookie_name : string; [@key "cookieName"] [@ocaml.doc "Name of the cookie to remove."]
    url : string; [@key "url"] [@ocaml.doc "URL to match cooke domain and path."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Deletes browser cookie with given name, domain and path."]
[@@ocaml.deprecated "deprecated in CDP, redirected to the Network domain"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Disable = struct
  let name = "Page.disable"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Disables page domain notifications."]

module Enable = struct
  let name = "Page.enable"

  type params = {
    enable_file_chooser_opened_event : bool option;
       [@key "enableFileChooserOpenedEvent"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If true, the `Page.fileChooserOpened` event will be emitted regardless of the state set by\n\
          `Page.setInterceptFileChooserDialog` command (default: false)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Enables page domain notifications."]

module Get_app_manifest = struct
  let name = "Page.getAppManifest"

  type params = { manifest_id : string option [@key "manifestId"] [@option] [@json.drop_default] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    url : string; [@key "url"] [@ocaml.doc "Manifest location."]
    errors : app_manifest_error list; [@key "errors"]
    data : string option; [@key "data"] [@option] [@json.drop_default] [@ocaml.doc "Manifest content."]
    parsed : app_manifest_parsed_properties option;
       [@key "parsed"]
       [@option]
       [@json.drop_default]
       [@ocaml.deprecated "deprecated in CDP"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Parsed manifest properties. Deprecated, use manifest instead."]
    manifest : web_app_manifest; [@key "manifest"] [@alert experimental "experimental in CDP, may change with Chrome"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Gets the processed manifest for this current document.\n\
  \  This API always waits for the manifest to be loaded.\n\
  \  If manifestId is provided, and it does not match the manifest of the\n\
  \    current document, this API errors out.\n\
  \  If there is not a loaded page, this API errors out immediately."]

module Get_installability_errors = struct
  let name = "Page.getInstallabilityErrors"

  type result = { installability_errors : installability_error list [@key "installabilityErrors"] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_manifest_icons = struct
  let name = "Page.getManifestIcons"

  type result = { primary_icon : string option [@key "primaryIcon"] [@option] [@json.drop_default] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc
  "Deprecated because it's not guaranteed that the returned icon is in fact the one used for PWA installation."]
[@@ocaml.deprecated "deprecated in CDP"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_app_id = struct
  let name = "Page.getAppId"

  type result = {
    app_id : string option;
       [@key "appId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "App id, either from manifest's id attribute or computed from start_url"]
    recommended_id : string option;
       [@key "recommendedId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Recommendation for manifest's id attribute to match current id computed from start_url"]
    bundle_id : string option;
       [@key "bundleId"] [@option] [@json.drop_default] [@ocaml.doc "The bundle ID for an Isolated Web App (IWA)"]
    parent_app_name : string option;
       [@key "parentAppName"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The name of the parent app if this app is a Sub-App"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc
  "Returns the unique (PWA) app id, along with IWA bundle ID and parent app info.\n\
   Only returns values if the feature flag 'WebAppEnableManifestId' is enabled"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_sub_apps = struct
  let name = "Page.getSubApps"

  type result = { sub_apps : sub_app list [@key "subApps"] } [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Returns the list of installed child Sub-Apps for the inspected parent app."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_sibling_sub_apps = struct
  let name = "Page.getSiblingSubApps"

  type result = { sub_apps : sub_app list [@key "subApps"] } [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Returns the list of sibling Sub-Apps sharing the same parent app if the inspected context is a Sub-App."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_ad_script_ancestry = struct
  let name = "Page.getAdScriptAncestry"

  type params = { frame_id : Cdp_base.Page.Frame_id.t [@key "frameId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    ad_script_ancestry : Cdp_network_types.ad_ancestry option;
       [@key "adScriptAncestry"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "The ancestry chain of ad script identifiers leading to this frame's\n\
          creation, along with the root script's filterlist rule. The ancestry\n\
          chain is ordered from the most immediate script (in the frame creation\n\
          stack) to more distant ancestors (that created the immediately preceding\n\
          script). Only sent if frame is labelled as an ad and ids are available."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_frame_tree = struct
  let name = "Page.getFrameTree"

  type result = { frame_tree : frame_tree [@key "frameTree"] [@ocaml.doc "Present frame tree structure."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Returns present frame tree structure."]

module Get_layout_metrics = struct
  let name = "Page.getLayoutMetrics"

  type result = {
    layout_viewport : layout_viewport;
       [@key "layoutViewport"]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc
         "Deprecated metrics relating to the layout viewport. Is in device pixels. Use `cssLayoutViewport` instead."]
    visual_viewport : visual_viewport;
       [@key "visualViewport"]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc
         "Deprecated metrics relating to the visual viewport. Is in device pixels. Use `cssVisualViewport` instead."]
    content_size : Cdp_dom_types.rect;
       [@key "contentSize"]
       [@ocaml.deprecated "deprecated in CDP"]
       [@ocaml.doc "Deprecated size of scrollable area. Is in DP. Use `cssContentSize` instead."]
    css_layout_viewport : layout_viewport;
       [@key "cssLayoutViewport"] [@ocaml.doc "Metrics relating to the layout viewport in CSS pixels."]
    css_visual_viewport : visual_viewport;
       [@key "cssVisualViewport"] [@ocaml.doc "Metrics relating to the visual viewport in CSS pixels."]
    css_content_size : Cdp_dom_types.rect; [@key "cssContentSize"] [@ocaml.doc "Size of scrollable area in CSS pixels."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Returns metrics relating to the layouting of the page, such as viewport bounds/scale."]

module Get_navigation_history = struct
  let name = "Page.getNavigationHistory"

  type result = {
    current_index : int; [@key "currentIndex"] [@ocaml.doc "Index of the current navigation history entry."]
    entries : navigation_entry list; [@key "entries"] [@ocaml.doc "Array of navigation history entries."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Returns navigation history for the current page."]

module Reset_navigation_history = struct
  let name = "Page.resetNavigationHistory"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Resets navigation history for the current page."]

module Get_resource_content = struct
  let name = "Page.getResourceContent"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Frame id to get resource for."]
    url : string; [@key "url"] [@ocaml.doc "URL of the resource to get content for."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    content : string; [@key "content"] [@ocaml.doc "Resource content."]
    base64_encoded : bool; [@key "base64Encoded"] [@ocaml.doc "True, if content was served as base64."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Returns content of the given resource."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_resource_tree = struct
  let name = "Page.getResourceTree"

  type result = {
    frame_tree : frame_resource_tree; [@key "frameTree"] [@ocaml.doc "Present frame / resource tree structure."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Returns present frame / resource tree structure."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Handle_java_script_dialog = struct
  let name = "Page.handleJavaScriptDialog"

  type params = {
    accept : bool; [@key "accept"] [@ocaml.doc "Whether to accept or dismiss the dialog."]
    prompt_text : string option;
       [@key "promptText"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The text to enter into the dialog prompt before accepting. Used only if this is a prompt\ndialog."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Accepts or dismisses a JavaScript initiated dialog (alert, confirm, prompt, or onbeforeunload)."]

module Navigate = struct
  let name = "Page.navigate"

  type params = {
    url : string; [@key "url"] [@ocaml.doc "URL to navigate the page to."]
    referrer : string option; [@key "referrer"] [@option] [@json.drop_default] [@ocaml.doc "Referrer URL."]
    transition_type : transition_type option;
       [@key "transitionType"] [@option] [@json.drop_default] [@ocaml.doc "Intended transition type."]
    frame_id : Cdp_base.Page.Frame_id.t option;
       [@key "frameId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Frame id to navigate, if not specified navigates the top frame."]
    referrer_policy : referrer_policy option;
       [@key "referrerPolicy"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Referrer-policy used for the navigation."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    frame_id : Cdp_base.Page.Frame_id.t;
       [@key "frameId"] [@ocaml.doc "Frame id that has navigated (or failed to navigate)"]
    loader_id : Cdp_base.Network.Loader_id.t option;
       [@key "loaderId"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Loader identifier. This is omitted in case of same-document navigation,\n\
          as the previously committed loaderId would not change."]
    error_text : string option;
       [@key "errorText"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "User friendly error message, present if and only if navigation has failed."]
    is_download : bool option;
       [@key "isDownload"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Whether the navigation resulted in a download."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Navigates current page to the given URL."]

module Navigate_to_history_entry = struct
  let name = "Page.navigateToHistoryEntry"

  type params = { entry_id : int [@key "entryId"] [@ocaml.doc "Unique id of the entry to navigate to."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Navigates current page to the given history entry."]

module Print_to_pdf = struct
  let name = "Page.printToPDF"

  type transfer_mode =
    | ReturnAsBase64 [@json.name "ReturnAsBase64"]
    | ReturnAsStream [@json.name "ReturnAsStream"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    landscape : bool option;
       [@key "landscape"] [@option] [@json.drop_default] [@ocaml.doc "Paper orientation. Defaults to false."]
    display_header_footer : bool option;
       [@key "displayHeaderFooter"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Display header and footer. Defaults to false."]
    print_background : bool option;
       [@key "printBackground"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Print background graphics. Defaults to false."]
    scale : Cdp_json.number option;
       [@key "scale"] [@option] [@json.drop_default] [@ocaml.doc "Scale of the webpage rendering. Defaults to 1."]
    paper_width : Cdp_json.number option;
       [@key "paperWidth"] [@option] [@json.drop_default] [@ocaml.doc "Paper width in inches. Defaults to 8.5 inches."]
    paper_height : Cdp_json.number option;
       [@key "paperHeight"] [@option] [@json.drop_default] [@ocaml.doc "Paper height in inches. Defaults to 11 inches."]
    margin_top : Cdp_json.number option;
       [@key "marginTop"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Top margin in inches. Defaults to 1cm (~0.4 inches)."]
    margin_bottom : Cdp_json.number option;
       [@key "marginBottom"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Bottom margin in inches. Defaults to 1cm (~0.4 inches)."]
    margin_left : Cdp_json.number option;
       [@key "marginLeft"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Left margin in inches. Defaults to 1cm (~0.4 inches)."]
    margin_right : Cdp_json.number option;
       [@key "marginRight"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Right margin in inches. Defaults to 1cm (~0.4 inches)."]
    page_ranges : string option;
       [@key "pageRanges"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Paper ranges to print, one based, e.g., '1-5, 8, 11-13'. Pages are\n\
          printed in the document order, not in the order specified, and no\n\
          more than once.\n\
          Defaults to empty string, which implies the entire document is printed.\n\
          The page numbers are quietly capped to actual page count of the\n\
          document, and ranges beyond the end of the document are ignored.\n\
          If this results in no pages to print, an error is reported.\n\
          It is an error to specify a range with start greater than end."]
    header_template : string option;
       [@key "headerTemplate"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "HTML template for the print header. Should be valid HTML markup with following\n\
          classes used to inject printing values into them:\n\
          - `date`: formatted print date\n\
          - `title`: document title\n\
          - `url`: document location\n\
          - `pageNumber`: current page number\n\
          - `totalPages`: total pages in the document\n\n\
          For example, `<span class=title></span>` would generate span containing the title."]
    footer_template : string option;
       [@key "footerTemplate"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "HTML template for the print footer. Should use the same format as the `headerTemplate`."]
    prefer_css_page_size : bool option;
       [@key "preferCSSPageSize"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Whether or not to prefer page size as defined by css. Defaults to false,\n\
          in which case the content will be scaled to fit the paper size."]
    transfer_mode : transfer_mode option;
       [@key "transferMode"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "return as stream"]
    generate_tagged_pdf : bool option;
       [@key "generateTaggedPDF"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Whether or not to generate tagged (accessible) PDF. Defaults to embedder choice."]
    generate_document_outline : bool option;
       [@key "generateDocumentOutline"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Whether or not to embed the document outline into the PDF."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    data : string;
       [@key "data"]
       [@ocaml.doc
         "Base64-encoded pdf data. Empty if |returnAsStream| is specified. (Encoded as a base64 string when passed \
          over JSON)"]
    stream : Cdp_base.Io.Stream_handle.t option;
       [@key "stream"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "A handle of the stream that holds resulting PDF data."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Print page as PDF."]

module Reload = struct
  let name = "Page.reload"

  type params = {
    ignore_cache : bool option;
       [@key "ignoreCache"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "If true, browser cache is ignored (as if the user pressed Shift+refresh)."]
    script_to_evaluate_on_load : string option;
       [@key "scriptToEvaluateOnLoad"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "If set, the script will be injected into all frames of the inspected page after reload.\n\
          Argument will be ignored if reloading dataURL origin."]
    loader_id : Cdp_base.Network.Loader_id.t option;
       [@key "loaderId"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If set, an error will be thrown if the target page's main frame's\n\
          loader id does not match the provided id. This prevents accidentally\n\
          reloading an unintended target in case there's a racing navigation."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Reloads given page optionally ignoring the cache."]

module Remove_script_to_evaluate_on_load = struct
  let name = "Page.removeScriptToEvaluateOnLoad"

  type params = { identifier : Cdp_base.Page.Script_identifier.t [@key "identifier"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Deprecated, please use removeScriptToEvaluateOnNewDocument instead."]
[@@ocaml.deprecated "deprecated in CDP"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Remove_script_to_evaluate_on_new_document = struct
  let name = "Page.removeScriptToEvaluateOnNewDocument"

  type params = { identifier : Cdp_base.Page.Script_identifier.t [@key "identifier"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Removes given script from the list."]

module Screencast_frame_ack = struct
  let name = "Page.screencastFrameAck"

  type params = { session_id : int [@key "sessionId"] [@ocaml.doc "Frame number."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Acknowledges that a screencast frame has been received by the frontend."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Search_in_resource = struct
  let name = "Page.searchInResource"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Frame id for resource to search in."]
    url : string; [@key "url"] [@ocaml.doc "URL of the resource to search in."]
    query : string; [@key "query"] [@ocaml.doc "String to search for."]
    case_sensitive : bool option;
       [@key "caseSensitive"] [@option] [@json.drop_default] [@ocaml.doc "If true, search is case sensitive."]
    is_regex : bool option;
       [@key "isRegex"] [@option] [@json.drop_default] [@ocaml.doc "If true, treats string parameter as regex."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { result : Cdp_debugger_types.search_match list [@key "result"] [@ocaml.doc "List of search matches."] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Searches for given string in resource content."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_ad_blocking_enabled = struct
  let name = "Page.setAdBlockingEnabled"

  type params = { enabled : bool [@key "enabled"] [@ocaml.doc "Whether to block ads."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Enable Chrome's experimental ad filter on all sites."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_bypass_csp = struct
  let name = "Page.setBypassCSP"

  type params = { enabled : bool [@key "enabled"] [@ocaml.doc "Whether to bypass page CSP."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Enable page Content Security Policy by-passing."]

module Get_permissions_policy_state = struct
  let name = "Page.getPermissionsPolicyState"

  type params = { frame_id : Cdp_base.Page.Frame_id.t [@key "frameId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { states : permissions_policy_feature_state list [@key "states"] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Get Permissions Policy state on given frame."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_origin_trials = struct
  let name = "Page.getOriginTrials"

  type params = { frame_id : Cdp_base.Page.Frame_id.t [@key "frameId"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = { origin_trials : origin_trial list [@key "originTrials"] }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Get Origin Trials on given frame."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_device_metrics_override = struct
  let name = "Page.setDeviceMetricsOverride"

  type params = {
    width : int;
       [@key "width"]
       [@ocaml.doc "Overriding width value in pixels (minimum 0, maximum 10000000). 0 disables the override."]
    height : int;
       [@key "height"]
       [@ocaml.doc "Overriding height value in pixels (minimum 0, maximum 10000000). 0 disables the override."]
    device_scale_factor : Cdp_json.number;
       [@key "deviceScaleFactor"] [@ocaml.doc "Overriding device scale factor value. 0 disables the override."]
    mobile : bool;
       [@key "mobile"]
       [@ocaml.doc
         "Whether to emulate mobile device. This includes viewport meta tag, overlay scrollbars, text\n\
          autosizing and more."]
    scale : Cdp_json.number option;
       [@key "scale"] [@option] [@json.drop_default] [@ocaml.doc "Scale to apply to resulting view image."]
    screen_width : int option;
       [@key "screenWidth"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Overriding screen width value in pixels (minimum 0, maximum 10000000)."]
    screen_height : int option;
       [@key "screenHeight"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Overriding screen height value in pixels (minimum 0, maximum 10000000)."]
    position_x : int option;
       [@key "positionX"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Overriding view X position on screen in pixels (minimum 0, maximum 10000000)."]
    position_y : int option;
       [@key "positionY"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Overriding view Y position on screen in pixels (minimum 0, maximum 10000000)."]
    dont_set_visible_size : bool option;
       [@key "dontSetVisibleSize"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Do not set visible view size, rely upon explicit setVisibleSize call."]
    screen_orientation : Cdp_emulation_types.screen_orientation option;
       [@key "screenOrientation"] [@option] [@json.drop_default] [@ocaml.doc "Screen orientation override."]
    viewport : viewport option;
       [@key "viewport"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The viewport dimensions and scale. If not set, the override is cleared."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Overrides the values of device screen dimensions (window.screen.width, window.screen.height,\n\
   window.innerWidth, window.innerHeight, and \"device-width\"/\"device-height\"-related CSS media\n\
   query results)."]
[@@ocaml.deprecated "deprecated in CDP, redirected to the Emulation domain"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_device_orientation_override = struct
  let name = "Page.setDeviceOrientationOverride"

  type params = {
    alpha : Cdp_json.number; [@key "alpha"] [@ocaml.doc "Mock alpha"]
    beta : Cdp_json.number; [@key "beta"] [@ocaml.doc "Mock beta"]
    gamma : Cdp_json.number; [@key "gamma"] [@ocaml.doc "Mock gamma"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Overrides the Device Orientation."]
[@@ocaml.deprecated "deprecated in CDP, redirected to the DeviceOrientation domain"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_font_families = struct
  let name = "Page.setFontFamilies"

  type params = {
    font_families : font_families;
       [@key "fontFamilies"]
       [@ocaml.doc "Specifies font families to set. If a font family is not specified, it won't be changed."]
    for_scripts : script_font_families list option;
       [@key "forScripts"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Specifies font families to set for individual scripts."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Set generic font families."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_font_sizes = struct
  let name = "Page.setFontSizes"

  type params = {
    font_sizes : font_sizes;
       [@key "fontSizes"]
       [@ocaml.doc "Specifies font sizes to set. If a font size is not specified, it won't be changed."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Set default font sizes."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_document_content = struct
  let name = "Page.setDocumentContent"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Frame id to set HTML for."]
    html : string; [@key "html"] [@ocaml.doc "HTML content to set."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Sets given markup as the document's HTML."]

module Set_download_behavior = struct
  let name = "Page.setDownloadBehavior"

  type behavior =
    | Deny [@json.name "deny"]
    | Allow [@json.name "allow"]
    | Default [@json.name "default"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    behavior : behavior;
       [@key "behavior"]
       [@ocaml.doc
         "Whether to allow all or deny all download requests, or use default Chrome behavior if\n\
          available (otherwise deny)."]
    download_path : string option;
       [@key "downloadPath"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "The default path to save downloaded files to. This is required if behavior is set to 'allow'"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Set the behavior when downloading a file."]
[@@ocaml.deprecated "deprecated in CDP"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_geolocation_override = struct
  let name = "Page.setGeolocationOverride"

  type params = {
    latitude : Cdp_json.number option; [@key "latitude"] [@option] [@json.drop_default] [@ocaml.doc "Mock latitude"]
    longitude : Cdp_json.number option; [@key "longitude"] [@option] [@json.drop_default] [@ocaml.doc "Mock longitude"]
    accuracy : Cdp_json.number option; [@key "accuracy"] [@option] [@json.drop_default] [@ocaml.doc "Mock accuracy"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Overrides the Geolocation Position or Error. Omitting any of the parameters emulates position\nunavailable."]
[@@ocaml.deprecated "deprecated in CDP, redirected to the Emulation domain"]

module Set_lifecycle_events_enabled = struct
  let name = "Page.setLifecycleEventsEnabled"

  type params = { enabled : bool [@key "enabled"] [@ocaml.doc "If true, starts emitting lifecycle events."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Controls whether page will emit lifecycle events."]

module Set_touch_emulation_enabled = struct
  let name = "Page.setTouchEmulationEnabled"

  type configuration =
    | Mobile [@json.name "mobile"]
    | Desktop [@json.name "desktop"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    enabled : bool; [@key "enabled"] [@ocaml.doc "Whether the touch event emulation should be enabled."]
    configuration : configuration option;
       [@key "configuration"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Touch/gesture events configuration. Default: current platform."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Toggles mouse event-based touch event emulation."]
[@@ocaml.deprecated "deprecated in CDP, redirected to the Emulation domain"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Start_screencast = struct
  let name = "Page.startScreencast"

  type format =
    | Jpeg [@json.name "jpeg"]
    | Png [@json.name "png"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    format : format option; [@key "format"] [@option] [@json.drop_default] [@ocaml.doc "Image compression format."]
    quality : int option;
       [@key "quality"] [@option] [@json.drop_default] [@ocaml.doc "Compression quality from range \\[0..100\\]."]
    max_width : int option; [@key "maxWidth"] [@option] [@json.drop_default] [@ocaml.doc "Maximum screenshot width."]
    max_height : int option; [@key "maxHeight"] [@option] [@json.drop_default] [@ocaml.doc "Maximum screenshot height."]
    every_nth_frame : int option;
       [@key "everyNthFrame"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Send every n-th frame. Must be a positive integer."]
    max_frames_in_flight : int option;
       [@key "maxFramesInFlight"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "Maximum number of frames sent until screencastFrameAck is required.\n\
          Defaults to 3. Must be a positive integer."]
    send_last_frame : bool option;
       [@key "sendLastFrame"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc
         "By default, after screencastFrameAck arrives, the next produced frame is sent.\n\
          Passing this flag enables storing the last produced frame in memory, which is\n\
          immediately sent upon screencastFrameAck. This way, overall performance is\n\
          traded for a better latency."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Starts sending each frame using the `screencastFrame` event."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Start_screen_recording = struct
  let name = "Page.startScreenRecording"

  type params = {
    audio : bool option; [@key "audio"] [@option] [@json.drop_default]
    max_width : int option;
       [@key "maxWidth"] [@option] [@json.drop_default] [@ocaml.doc "Maximum frame width in pixels."]
    max_height : int option;
       [@key "maxHeight"] [@option] [@json.drop_default] [@ocaml.doc "Maximum frame height in pixels."]
    frame_rate : int option;
       [@key "frameRate"] [@option] [@json.drop_default] [@ocaml.doc "Maximum frame rate in frames per second."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    stream : Cdp_base.Io.Stream_handle.t;
       [@key "stream"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "A handle of the stream that holds resulting screencast data."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Starts screencast video recording."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Stop_screen_recording = struct
  let name = "Page.stopScreenRecording"

  type result = {
    stream : Cdp_base.Io.Stream_handle.t;
       [@key "stream"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "A handle of the stream that holds resulting screencast data."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Stops screencast video recording."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Stop_loading = struct
  let name = "Page.stopLoading"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Force the page stop all navigations and pending resource fetches."]

module Crash = struct
  let name = "Page.crash"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Crashes renderer on the IO thread, generates minidumps."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Close = struct
  let name = "Page.close"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Tries to close page, running its beforeunload hooks, if any."]

module Set_web_lifecycle_state = struct
  let name = "Page.setWebLifecycleState"

  type state =
    | Frozen [@json.name "frozen"]
    | Active [@json.name "active"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = { state : state [@key "state"] [@ocaml.doc "Target lifecycle state"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Tries to update the web lifecycle state of the page.\n\
   It will transition the page to the given state according to:\n\
   https://github.com/WICG/web-lifecycle/"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Stop_screencast = struct
  let name = "Page.stopScreencast"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Stops sending each frame in the `screencastFrame`."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Produce_compilation_cache = struct
  let name = "Page.produceCompilationCache"

  type params = { scripts : compilation_cache_params list [@key "scripts"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Requests backend to produce compilation cache for the specified scripts.\n\
   `scripts` are appended to the list of scripts for which the cache\n\
   would be produced. The list may be reset during page navigation.\n\
   When script with a matching URL is encountered, the cache is optionally\n\
   produced upon backend discretion, based on internal heuristics.\n\
   See also: `Page.compilationCacheProduced`."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Add_compilation_cache = struct
  let name = "Page.addCompilationCache"

  type params = {
    url : string; [@key "url"]
    data : string; [@key "data"] [@ocaml.doc "Base64-encoded data (Encoded as a base64 string when passed over JSON)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Seeds compilation cache for given url. Compilation cache does not survive\ncross-process navigation."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Clear_compilation_cache = struct
  let name = "Page.clearCompilationCache"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Clears seeded compilation cache."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_spc_transaction_mode = struct
  let name = "Page.setSPCTransactionMode"

  type mode =
    | None_ [@json.name "none"]
    | AutoAccept [@json.name "autoAccept"]
    | AutoChooseToAuthAnotherWay [@json.name "autoChooseToAuthAnotherWay"]
    | AutoReject [@json.name "autoReject"]
    | AutoOptOut [@json.name "autoOptOut"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = { mode : mode [@key "mode"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Sets the Secure Payment Confirmation transaction mode.\n\
   https://w3c.github.io/secure-payment-confirmation/#sctn-automation-set-spc-transaction-mode"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_rph_registration_mode = struct
  let name = "Page.setRPHRegistrationMode"

  type mode =
    | None_ [@json.name "none"]
    | AutoAccept [@json.name "autoAccept"]
    | AutoReject [@json.name "autoReject"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = { mode : mode [@key "mode"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Extensions for Custom Handlers API:\nhttps://html.spec.whatwg.org/multipage/system-state.html#rph-automation"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Generate_test_report = struct
  let name = "Page.generateTestReport"

  type params = {
    message : string; [@key "message"] [@ocaml.doc "Message to be displayed in the report."]
    group : string option;
       [@key "group"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Specifies the endpoint group to deliver the report to."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc "Generates a report for testing."] [@@alert experimental "experimental in CDP, may change with Chrome"]

module Wait_for_debugger = struct
  let name = "Page.waitForDebugger"

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command : result Cdp_command.t = { Cdp_command.name; params = None; parse = result_of_json }
end
[@@ocaml.doc "Pauses page execution. Can be resumed using generic Runtime.runIfWaitingForDebugger."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Set_intercept_file_chooser_dialog = struct
  let name = "Page.setInterceptFileChooserDialog"

  type params = {
    enabled : bool; [@key "enabled"]
    cancel : bool option;
       [@key "cancel"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc
         "If true, cancels the dialog by emitting relevant events (if any)\n\
          in addition to not showing it if the interception is enabled\n\
          (default: false)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Intercept file chooser requests and transfer control to protocol clients.\n\
   When file chooser interception is enabled, native file chooser dialog is not shown.\n\
   Instead, a protocol event `Page.fileChooserOpened` is emitted."]

module Set_prerendering_allowed = struct
  let name = "Page.setPrerenderingAllowed"

  type params = { is_allowed : bool [@key "isAllowed"] } [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = unit [@@deriving show, eq]

  let result_of_json (_ignored_payload : Cdp_json.t) : result = ()

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Enable/disable prerendering manually.\n\n\
   This command is a short-term solution for https://crbug.com/1440085.\n\
   See https://docs.google.com/document/d/12HVmFxYj5Jc-eJr5OmWsa2bqTJsbgGLKI6ZIyx0_wpA\n\
   for more details.\n\n\
   TODO(https://crbug.com/1440085): Remove this once Puppeteer supports tab targets."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Get_annotated_page_content = struct
  let name = "Page.getAnnotatedPageContent"

  type params = {
    include_actionable_information : bool option;
       [@key "includeActionableInformation"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Whether to include actionable information. Defaults to true."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  type result = {
    content : string;
       [@key "content"]
       [@ocaml.doc
         "The annotated page content as a base64 encoded protobuf.\n\
          The format is defined by the `AnnotatedPageContent` message in\n\
          components/optimization_guide/proto/features/common_quality_data.proto (Encoded as a base64 string when \
          passed over JSON)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq]

  let command params : result Cdp_command.t =
    { Cdp_command.name; params = Some (params_to_json params); parse = result_of_json }
end
[@@ocaml.doc
  "Get the annotated page content for the main frame.\nThis is an experimental command that is subject to change."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Dom_content_event_fired = struct
  let name = "Page.domContentEventFired"

  type params = { timestamp : Cdp_base.Network.Monotonic_time.t [@key "timestamp"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end

module File_chooser_opened = struct
  let name = "Page.fileChooserOpened"

  type mode =
    | SelectSingle [@json.name "selectSingle"]
    | SelectMultiple [@json.name "selectMultiple"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t;
       [@key "frameId"]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Id of the frame containing input node."]
    mode : mode; [@key "mode"] [@ocaml.doc "Input mode."]
    backend_node_id : Cdp_base.Dom.Backend_node_id.t option;
       [@key "backendNodeId"]
       [@option]
       [@json.drop_default]
       [@alert experimental "experimental in CDP, may change with Chrome"]
       [@ocaml.doc "Input node id. Only present for file choosers opened via an `<input type=\"file\">` element."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Emitted only when `page.interceptFileChooser` is enabled."]

module Frame_attached = struct
  let name = "Page.frameAttached"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Id of the frame that has been attached."]
    parent_frame_id : Cdp_base.Page.Frame_id.t; [@key "parentFrameId"] [@ocaml.doc "Parent frame identifier."]
    stack : Cdp_runtime_types.stack_trace option;
       [@key "stack"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "JavaScript stack trace of when frame was attached, only set if frame initiated from script."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when frame has been attached to its parent."]

module Frame_cleared_scheduled_navigation = struct
  let name = "Page.frameClearedScheduledNavigation"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t;
       [@key "frameId"] [@ocaml.doc "Id of the frame that has cleared its scheduled navigation."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when frame no longer has a scheduled navigation."] [@@ocaml.deprecated "deprecated in CDP"]

module Frame_detached = struct
  let name = "Page.frameDetached"

  type reason =
    | Remove [@json.name "remove"]
    | Swap [@json.name "swap"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Id of the frame that has been detached."]
    reason : reason; [@key "reason"] [@alert experimental "experimental in CDP, may change with Chrome"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when frame has been detached from its parent."]

module Frame_subtree_will_be_detached = struct
  let name = "Page.frameSubtreeWillBeDetached"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t;
       [@key "frameId"] [@ocaml.doc "Id of the frame that is the root of the subtree that will be detached."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired before frame subtree is detached. Emitted before any frame of the\nsubtree is actually detached."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Frame_navigated = struct
  let name = "Page.frameNavigated"

  type params = {
    frame : frame; [@key "frame"] [@ocaml.doc "Frame object."]
    type_ : navigation_type; [@key "type"] [@alert experimental "experimental in CDP, may change with Chrome"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired once navigation of the frame has completed. Frame is now associated with the new loader."]

module Document_opened = struct
  let name = "Page.documentOpened"

  type params = { frame : frame [@key "frame"] [@ocaml.doc "Frame object."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when opening document to write to."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Frame_resized = struct
  let name = "Page.frameResized"

  type params = unit [@@deriving show, eq]

  let params_of_json (_ignored_payload : Cdp_json.t) : params = ()

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Frame_started_navigating = struct
  let name = "Page.frameStartedNavigating"

  type frame_started_navigating_navigation_type =
    | Reload [@json.name "reload"]
    | ReloadBypassingCache [@json.name "reloadBypassingCache"]
    | Restore [@json.name "restore"]
    | RestoreWithPost [@json.name "restoreWithPost"]
    | HistorySameDocument [@json.name "historySameDocument"]
    | HistoryDifferentDocument [@json.name "historyDifferentDocument"]
    | SameDocument [@json.name "sameDocument"]
    | DifferentDocument [@json.name "differentDocument"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "ID of the frame that is being navigated."]
    url : string; [@key "url"] [@ocaml.doc "The URL the navigation started with. The final URL can be different."]
    loader_id : Cdp_base.Network.Loader_id.t;
       [@key "loaderId"]
       [@ocaml.doc
         "Loader identifier. Even though it is present in case of same-document\n\
          navigation, the previously committed loaderId would not change unless\n\
          the navigation changes from a same-document to a cross-document\n\
          navigation."]
    navigation_type : frame_started_navigating_navigation_type; [@key "navigationType"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired when a navigation starts. This event is fired for both\n\
   renderer-initiated and browser-initiated navigations. For renderer-initiated\n\
   navigations, the event is fired after `frameRequestedNavigation`.\n\
   Navigation may still be cancelled after the event is issued. Multiple events\n\
   can be fired for a single navigation, for example, when a same-document\n\
   navigation becomes a cross-document navigation (such as in the case of a\n\
   frameset)."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Frame_requested_navigation = struct
  let name = "Page.frameRequestedNavigation"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Id of the frame that is being navigated."]
    reason : client_navigation_reason; [@key "reason"] [@ocaml.doc "The reason for the navigation."]
    url : string; [@key "url"] [@ocaml.doc "The destination URL for the requested navigation."]
    disposition : client_navigation_disposition; [@key "disposition"] [@ocaml.doc "The disposition for the navigation."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired when a renderer-initiated navigation is requested.\n\
   Navigation may still be cancelled after the event is issued."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Frame_scheduled_navigation = struct
  let name = "Page.frameScheduledNavigation"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t;
       [@key "frameId"] [@ocaml.doc "Id of the frame that has scheduled a navigation."]
    delay : Cdp_json.number;
       [@key "delay"]
       [@ocaml.doc
         "Delay (in seconds) until the navigation is scheduled to begin. The navigation is not\nguaranteed to start."]
    reason : client_navigation_reason; [@key "reason"] [@ocaml.doc "The reason for the navigation."]
    url : string; [@key "url"] [@ocaml.doc "The destination URL for the scheduled navigation."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when frame schedules a potential navigation."] [@@ocaml.deprecated "deprecated in CDP"]

module Frame_started_loading = struct
  let name = "Page.frameStartedLoading"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Id of the frame that has started loading."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when frame has started loading."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Frame_stopped_loading = struct
  let name = "Page.frameStoppedLoading"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Id of the frame that has stopped loading."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when frame has stopped loading."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Download_will_begin = struct
  let name = "Page.downloadWillBegin"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Id of the frame that caused download to begin."]
    guid : string; [@key "guid"] [@ocaml.doc "Global unique identifier of the download."]
    url : string; [@key "url"] [@ocaml.doc "URL of the resource being downloaded."]
    suggested_filename : string;
       [@key "suggestedFilename"]
       [@ocaml.doc "Suggested file name of the resource (the actual name of the file saved on disk may differ)."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when page is about to start a download.\nDeprecated. Use Browser.downloadWillBegin instead."]
[@@ocaml.deprecated "deprecated in CDP"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Download_progress = struct
  let name = "Page.downloadProgress"

  type state =
    | InProgress [@json.name "inProgress"]
    | Completed [@json.name "completed"]
    | Canceled [@json.name "canceled"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    guid : string; [@key "guid"] [@ocaml.doc "Global unique identifier of the download."]
    total_bytes : Cdp_json.number; [@key "totalBytes"] [@ocaml.doc "Total expected bytes to download."]
    received_bytes : Cdp_json.number; [@key "receivedBytes"] [@ocaml.doc "Total bytes received."]
    state : state; [@key "state"] [@ocaml.doc "Download status."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired when download makes progress. Last call has |done| == true.\nDeprecated. Use Browser.downloadProgress instead."]
[@@ocaml.deprecated "deprecated in CDP"]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Interstitial_hidden = struct
  let name = "Page.interstitialHidden"

  type params = unit [@@deriving show, eq]

  let params_of_json (_ignored_payload : Cdp_json.t) : params = ()

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when interstitial page was hidden"]

module Interstitial_shown = struct
  let name = "Page.interstitialShown"

  type params = unit [@@deriving show, eq]

  let params_of_json (_ignored_payload : Cdp_json.t) : params = ()

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when interstitial page was shown"]

module Javascript_dialog_closed = struct
  let name = "Page.javascriptDialogClosed"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t;
       [@key "frameId"] [@alert experimental "experimental in CDP, may change with Chrome"] [@ocaml.doc "Frame id."]
    result : bool; [@key "result"] [@ocaml.doc "Whether dialog was confirmed."]
    user_input : string; [@key "userInput"] [@ocaml.doc "User input in case of prompt."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when a JavaScript initiated dialog (alert, confirm, prompt, or onbeforeunload) has been\nclosed."]

module Javascript_dialog_opening = struct
  let name = "Page.javascriptDialogOpening"

  type params = {
    url : string; [@key "url"] [@ocaml.doc "Frame url."]
    frame_id : Cdp_base.Page.Frame_id.t;
       [@key "frameId"] [@alert experimental "experimental in CDP, may change with Chrome"] [@ocaml.doc "Frame id."]
    message : string; [@key "message"] [@ocaml.doc "Message that will be displayed by the dialog."]
    type_ : dialog_type; [@key "type"] [@ocaml.doc "Dialog type."]
    has_browser_handler : bool;
       [@key "hasBrowserHandler"]
       [@ocaml.doc
         "True iff browser is capable showing or acting on the given dialog. When browser has no\n\
          dialog handler for given target, calling alert while Page domain is engaged will stall\n\
          the page execution. Execution can be resumed via calling Page.handleJavaScriptDialog."]
    default_prompt : string option;
       [@key "defaultPrompt"] [@option] [@json.drop_default] [@ocaml.doc "Default dialog prompt."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when a JavaScript initiated dialog (alert, confirm, prompt, or onbeforeunload) is about to\nopen."]

module Lifecycle_event = struct
  let name = "Page.lifecycleEvent"

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Id of the frame."]
    loader_id : Cdp_base.Network.Loader_id.t;
       [@key "loaderId"] [@ocaml.doc "Loader identifier. Empty string if the request is fetched from worker."]
    name : string; [@key "name"]
    timestamp : Cdp_base.Network.Monotonic_time.t; [@key "timestamp"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired for lifecycle events (navigation, load, paint, etc) in the current\ntarget (including local frames)."]

module Back_forward_cache_not_used = struct
  let name = "Page.backForwardCacheNotUsed"

  type params = {
    loader_id : Cdp_base.Network.Loader_id.t;
       [@key "loaderId"] [@ocaml.doc "The loader id for the associated navigation."]
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "The frame id of the associated frame."]
    not_restored_explanations : back_forward_cache_not_restored_explanation list;
       [@key "notRestoredExplanations"]
       [@ocaml.doc "Array of reasons why the page could not be cached. This must not be empty."]
    not_restored_explanations_tree : back_forward_cache_not_restored_explanation_tree option;
       [@key "notRestoredExplanationsTree"]
       [@option]
       [@json.drop_default]
       [@ocaml.doc "Tree structure of reasons why the page could not be cached for each frame."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc
  "Fired for failed bfcache history navigations if BackForwardCache feature is enabled. Do\n\
   not assume any ordering with the Page.frameNavigated event. This event is fired only for\n\
   main-frame history navigation where the document changes (non-same-document navigations),\n\
   when bfcache navigation fails."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Load_event_fired = struct
  let name = "Page.loadEventFired"

  type params = { timestamp : Cdp_base.Network.Monotonic_time.t [@key "timestamp"] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end

module Navigated_within_document = struct
  let name = "Page.navigatedWithinDocument"

  type navigated_within_document_navigation_type =
    | Fragment [@json.name "fragment"]
    | HistoryApi [@json.name "historyApi"]
    | Other_ [@json.name "other"]
    | Other of Cdp_json.unknown [@json.catch_all]
  [@@compact_variants] [@@deriving json, show, eq]

  type params = {
    frame_id : Cdp_base.Page.Frame_id.t; [@key "frameId"] [@ocaml.doc "Id of the frame."]
    url : string; [@key "url"] [@ocaml.doc "Frame's new url."]
    navigation_type : navigated_within_document_navigation_type; [@key "navigationType"] [@ocaml.doc "Navigation type"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when same-document navigation happens, e.g. due to history API usage or anchor navigation."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Screencast_frame = struct
  let name = "Page.screencastFrame"

  type params = {
    data : string;
       [@key "data"] [@ocaml.doc "Base64-encoded compressed image. (Encoded as a base64 string when passed over JSON)"]
    metadata : screencast_frame_metadata; [@key "metadata"] [@ocaml.doc "Screencast frame metadata."]
    session_id : int; [@key "sessionId"] [@ocaml.doc "Frame number."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Compressed image data requested by the `startScreencast`."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Screencast_visibility_changed = struct
  let name = "Page.screencastVisibilityChanged"

  type params = { visible : bool [@key "visible"] [@ocaml.doc "True if the page is visible."] }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when the page with currently enabled screencast was shown or hidden `."]
[@@alert experimental "experimental in CDP, may change with Chrome"]

module Window_open = struct
  let name = "Page.windowOpen"

  type params = {
    url : string; [@key "url"] [@ocaml.doc "The URL for the new window."]
    window_name : string; [@key "windowName"] [@ocaml.doc "Window name."]
    window_features : string list; [@key "windowFeatures"] [@ocaml.doc "An array of enabled window features."]
    user_gesture : bool; [@key "userGesture"] [@ocaml.doc "Whether or not it was triggered by user gesture."]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Fired when a new window is going to be opened, via window.open(), link click, form submission,\netc."]

module Compilation_cache_produced = struct
  let name = "Page.compilationCacheProduced"

  type params = {
    url : string; [@key "url"]
    data : string; [@key "data"] [@ocaml.doc "Base64-encoded data (Encoded as a base64 string when passed over JSON)"]
  }
  [@@allow_extra_fields] [@@deriving json, show, eq, make]

  let event : params Cdp_event.t = { Cdp_event.name; parse = params_of_json }
end
[@@ocaml.doc "Issued for every compilation cache generated."]
[@@alert experimental "experimental in CDP, may change with Chrome"]
